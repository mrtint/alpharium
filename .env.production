# prod 환경 — 엔드유저에게 나가는 배포 빌드
#
# 온디바이스 추론만 쓴다(FR-012). 데스크톱 서버 주소 키를 두지 않는다 —
# 값을 비우는 것으로 대신하지 않는다. EXPO_PUBLIC_ 값은 배포본에 문자열로 박히고
# 엔드유저가 읽을 수 있으며, 키가 존재하면 언젠가 값이 채워진다(FR-014).
EXPO_PUBLIC_APP_ENV=prod
