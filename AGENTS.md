# 헌법을 먼저 읽는다

이 프로젝트가 무엇이고 무엇을 지켜야 하는지는 [.specify/memory/constitution.md](.specify/memory/constitution.md)에
있다. **작업을 시작하기 전에 그것을 읽는다.** 이 문서는 헌법을 요약하지 않으며,
헌법이 담지 않는 실무 사실만 남긴다. 둘이 어긋나면 헌법이 우선한다.

한 줄로 말하면: **주인의 휴대폰이 화자가 되어 하루를 일기로 쓰는 앱**이다. 사람이
쓰는 일기가 아니다. 나머지는 헌법에 있다.

## 저장소의 현재 상태

2026-08-12에 이전 작업 전체를 되돌렸다. 같은 날 헌법을 새로 세웠고(v1.0.2), 첫 기능인
프로젝트 뼈대를 세웠다. 2026-08-13에 두 번째 기능인 파이프라인 계약을 세웠다. 2026-08-14에 셋째 기능으로 캐릭터별
모델 파일 확보를 마쳤다. 2026-08-16에 넷째 기능으로 사진 신호 수집을 구현했다.
2026-08-17에 다섯째 기능으로 **실제 일기 생성**을 구현하고 **실기기에서 확인했다.**
2026-08-20에 여섯째 기능으로 **손에 쥐는 첫 빌드**를 마쳤다 — 사용자 화면(목록·상세·
쓰기), 실제 저장, 제 키로 서명한 release APK까지다. 2026-08-20~22에 007~011로
캐릭터 선택·대기·과거 하루 선택·합성 하루 심기·사진 내용 읽기를 차례로 더했다.
2026-08-22에 열두째 기능으로 **정오 이후 오늘을 쓸 수 있게** 했다.

- **헌법**: 있다. 원칙 I~V, 로스터, 시각 처리 설정까지 확정됐다.
- **스펙**: `specs/001-project-skeleton-setup/`부터 `specs/012-today-diary/`까지
  열두 개.
- **앱 코드**: 뼈대와 축 사이 계약에 사진 신호 수집, 일기 생성, 사용자 화면과
  저장·배포, 캐릭터 선택, 과거 하루 선택, 사진의 내용을 읽는 것, 그리고 **정오
  이후 오늘을 쓰는 것**까지 있다.

**006이 앱을 손에 쥐게 했다.** 그 전까지 일기는 진단 화면에서만 만들어졌고 **저장된
적이 한 번도 없었다** — `GenerationProbe`가 어댑터를 직접 불러 파이프라인을 건너뛰었기
때문이다. 지금은 이렇다:

- **진단 화면 없이 일기를 만들고 읽는다** — 목록·상세·쓰는 중이 `src/app/state.ts`의
  순수 함수로 갈리고, 화면은 그리기만 한다
- **일기가 파일로 남는다.** 앱을 껐다 켜도, 기기를 재부팅해도, **같은 키로 서명한 다음
  빌드를 덮어 설치해도** 그대로 있다
- **케이블을 뽑아도 돈다** — Metro를 끄고 `adb reverse`를 지운 채 release APK가 뜬다
- **제 키로 서명한다**(`CN=alpharium`). 키도 비밀번호도 저장소에 없다

**005가 `generate()`를 채웠다.** 지금의 상태는 이렇다:

- **일기를 실제로 생성한다**(005) — 프롬프트를 만들고, 모델을 열고, 돌리고, 판정하고,
  닫는다. `generate()`는 더 이상 `not-implemented`가 아니다
- **파이프라인이 `generation`을 지나 저장까지 간다.** 002 이후 처음이며, 대역 추론으로
  `ok: true`가 나오는 것을 테스트가 확인한다
- **✅ 실기기에서 일기가 나오는 것을 확인했다** (2026-08-17, SM-G986N). `initLlama`가
  실제로 열리고, 약 30초 뒤 한국어 일기가 나왔다. `npm run test:device`도 **PASSED**다
  (흐름 3개). **다만 생성된 글이 기록에 없는 것을 단언했다** — 프롬프트를 고칠 일이 남았다
- **사진은 실제로 수집한다**(004). 권한이 없으면 **빈 목록이 아니라 `unknown`이다**
- 걸음·배터리·연결은 **여전히 `unknown`이며 그것이 결론이다** — 되짚을 통로가 없다
- **사진의 내용은 보지 않는다.** 시각 설정이 `quick`/`detailed`면 **조용히 낮추지 않고
  실패를 돌려준다** — 사진을 보지 않은 일기를 대신 주지 않는다

경계면을 먼저 세운 이유는 축 하나를 깊게 파고드는 실패를 반복하지 않기 위해서다.

되돌린 이유는 실패해서가 아니라 **측정 장치와 제품이 뒤섞였기 때문**이다. 이전
저장소에는 모델 출력을 재고 판정하는 코드·문서가 제품 코드와 같은 자리에 쌓여
있었고, 그 결과 새 세션마다 이 저장소를 「측정하는 제품」으로 잘못 읽었다. 읽는
쪽의 실수가 아니라 저장소가 그렇게 보이게 되어 있었다. 이것이 헌법 원칙 IV가 된
경위다.

**이전 작업의 결론을 기억에서 꺼내 복원하지 않는다.** 헌법에 적힌 것만이 확정이다.
헌법에 없는 이전 결론은 되돌려진 것이며, 복원하면 되돌린 의미가 없어진다.

## 이전 작업에서 실측으로 확인된 것

값을 다시 확인하는 데 시간을 쓰지 않도록 사실만 남긴다. **이것은 규범이 아니며,
헌법이 무엇을 정하든 그쪽이 우선한다.**

- 온디바이스 추론은 실기기(Galaxy S20+, Android 13, arm64-v8a)에서 실제로 돌았다.
  그 구성은 Expo 57 + React Native 0.86 + `llama.rn`이었다.
- **Android에는 기간 걸음 수를 되짚는 통로가 없다.** `expo-sensors`의
  `getStepCountAsync`는 iOS 전용이다. 앱 실행 이후의 증분 구독은 「0시부터 누적」이
  아니므로 같은 것으로 쓸 수 없다. 그래서 걸음 수는 `unknown`이 **정상 상태**이며,
  0으로 채우면 「걷지 않았다」는 거짓이 된다(헌법 원칙 V).
- **사진 신호 수집이 실기기에서 돈다** (2026-08-16, SM-G986N, Android 13, **무선 adb**).
  `known — 3장`, 자리 `1곳 (사진 3장 중 3장에서)`. **권한을 거두면 `none`이 아니라
  `unknown`이 되는 것까지 확인했다** — 같은 사진이 그대로 있는데 「없다」가 아니라 「모른다」로
  바뀐다(원칙 V의 실전 검증). 되돌리면 `known`으로 복귀한다.
- **무선 adb로 실기기 테스트가 가능하다.** `adb mdns services`로 기기를 찾고
  `adb connect IP:포트`로 붙는다. `adb reverse tcp:8081 tcp:8081`도 무선에서 되므로 Metro를
  `localhost:8081`로 잡을 수 있다. **`expo run:android --device`는 `IP:포트`를 받지 못한다** —
  기기가 하나면 플래그를 뺀다.
- **⚠️ `expo run:android`만으로는 매니페스트가 갱신되지 않는다.** `android/`가 이미 있으면
  prebuild를 건너뛴다. 004에서 이것 때문에 `READ_MEDIA_IMAGES`와 `ACCESS_MEDIA_LOCATION`이
  **둘 다 빠진 APK**가 설치됐다. `npx expo prebuild --platform android --clean`이 필요하며,
  **`adb shell dumpsys package <패키지>`의 `requested permissions`로 확인한다** — 빌드
  성공이 매니페스트가 맞다는 뜻은 아니다.
- **`expo-media-library 57`은 `ACCESS_MEDIA_LOCATION`을 따로 묻는 함수를 주지 않는다**
  (2026-08-16, 설치본 타입 직접 확인). 이 권한은 `getLocation()`·`getExif()`의 주석에만
  나오고 조회 API가 없다. 그래서 **좌표 권한이 있는지는 실제로 읽어 봐야 안다** — 004는
  `locationOf()`가 전부 실패하면 `unknown`, `absent`가 섞이면 `none`으로 판정한다.
  `getLocation()`은 권한이 없을 때 **`null`이 아니라 예외를 던지므로** 반드시 감싼다.
- **`@testing-library/react-native` 14의 `render`는 Promise를 반환한다**(2026-08-16).
  `await` 없이 쓰면 `screen`이 비어 있고, 오류 문구가 "`render` function has not been
  called"라 원인을 가리키지 않는다. `toHaveTextContent`는 **정확히 일치**를 보므로 부분
  문자열은 정규식으로 준다.
- **⚠️ `react-native`의 `SafeAreaView`는 안드로이드에서 아무 일도 하지 않는다**
  (2026-08-20 실기기 실측). **iOS 전용**이며, 안드로이드에서는 그냥 `View`다.
  - 이 앱은 `android/gradle.properties`에 **`edgeToEdgeEnabled=true`**이고
    `styles.xml`이 상태 표시줄을 투명으로 두므로 **화면이 시스템 막대 아래까지
    그려진다.** 그래서 시계·배터리·블루투스 표시와 탭 글자가 **겹쳐 보였다.**
  - 고치는 법은 **`react-native-safe-area-context`**를 쓰는 것이다(SDK 57 기준
    `~5.7.0`, `expo install`이 고른다). 루트를 **`SafeAreaProvider`로 감싸야**
    `SafeAreaView`가 잴 값을 얻는다 — **감싸지 않으면 인셋이 0이라 고친 것처럼
    보이면서 그대로 겹친다.**
  - **조용히 실패하는 것이 이 버그의 성질이다**: 이름도 쓰임새도 맞아 보이고, iOS에서는
    실제로 동작하며, 빌드도 테스트도 통과한다. **안드로이드 화면을 눈으로 봐야 드러난다** —
    006의 실기기 확인이 전부 초록불이었는데도 이것을 놓쳤다.
  - **고친 뒤 release 빌드로 확인했다** (SM-G986N). 시계·배터리·블루투스가 제 띠에
    있고 탭은 그 아래에 있다. 목록·상세 둘 다 겹치지 않으며 **아래쪽 제스처 막대도
    자리를 가진다.** `react-native-safe-area-context`의 네이티브 모듈이 **R8·ProGuard를
    켠 release에서 동작한다.**
- **`llama.rn` 0.12.9의 API를 설치본 타입으로 직접 확인했다** (2026-08-17). 쓰는 것은
  `initLlama`·`completion`·`stopCompletion`·`release` 넷이다. **`completion()`의 결과에
  `timings`·`tokens_predicted`·`predicted_per_second`가 들어 있다** — 요청하지 않아도
  온다. 헌법 원칙 IV가 금지한 값이 네이티브에서 밀려 들어오므로 `llama-port.ts`가
  경계에서 버린다.
  - **`stopCompletion()`은 `completion()`을 거부시키지 않는다.** `interrupted: true`인
    결과로 **정상 resolve된다** — `try/catch`로 끊김을 잡으려 하면 놓친다.
    **그 결과에도 거기까지 생성된 `text`가 들어 있으므로** 판정이 `unfinished`를 가장
    먼저 본다.
  - **잘림을 짐작할 필요가 없다.** `stopped_limit`·`truncated`·`context_full`·
    `interrupted`가 사실을 준다. 문장 부호로 추측했다면 그것이 휴리스틱이고 임계값이며
    원칙 IV로 가는 길이었다.
- **일기 생성이 실기기에서 돈다** (2026-08-17, SM-G986N, Android 13, **USB adb**).
  `imaginative`(hyperclovax 1.5B Q4_K_M)로 확인했다. **002 이후 처음으로 파이프라인이
  `generation`을 지나 저장까지 갔다.**
  - **모델 적재 약 2.1초, 생성 약 30초** (CPU 전용, `n_threads=4`, 205토큰 프롬프트).
    **그래서 「앱이 앞에 있는 동안만 생성한다」를 유지한다** — 30초는 백그라운드 실행이라는
    축을 열 근거가 못 된다.
  - **`n_ctx` 2048로 충분했다** — `context_full`이 한 번도 나지 않았다. 다만 사진이 아주
    많은 하루는 아직 못 봤다.
  - **⚠️ 평문 `prompt`로는 빈 글만 나온다.** instruct 모델(hyperclovax)에 채팅 템플릿 없이
    평문을 넣으면 **즉시 EOS를 내고 아무것도 쓰지 않는다** — 1초도 안 걸리고 `rejected: empty`가
    된다. **`completion({ messages: [{ role: "user", content: prompt }], jinja: true })`로
    보내야 한다.** 이것을 모르면 「모델이 고장났다」로 오독한다.
  - **`result.content`를 먼저 본다.** `text`와 `content`가 따로 오고, 템플릿에 따라 어느
    쪽에 값이 오는지 다르다. 둘 다 비면 판정이 거부한다(지어내지 않는다).
  - **생성된 일기가 헌법 원칙 II를 완전히 지키지는 않았다.** 화자는 휴대폰이었고 시야의
    한계도 말했지만(SC-002 통과), **기록에 없는 것을 단언했다**(SC-002a 실패) — "날씨가
    정말 좋았다", "친구와 산책을 했다", "커피를 마시며". **고칠 자리는 프롬프트이며 판정
    갈래를 늘리지 않는다**(원칙 IV).
  - **~~끊김(앱이 앞을 벗어남)은 확인하지 못했다.~~ ⚠️ 이 기록이 틀렸다** (2026-08-20,
    007 계획 중 코드 확인). 「생성이 30초라 홈에 다녀오는 사이에 끝나 버린다」고 적었으나
    **진짜 이유는 배선이 끊겨 있어 확인할 수 없었다는 것이다.**
    - `on-device.ts`가 `StoppableBackend`를 만들고 `DiaryHomeScreen`이 `stop?`을 받는데,
      **그 사이의 `selectBackend()`가 `InferenceBackend`로 좁혀 `stop`을 타입에서
      지웠다.** `wiring.ts`도 backend를 버렸고, 그래서 **`App.tsx`가 넘길 것이 없었다.**
    - **조용히 통과한 이유**: `stop?`이 옵셔널이라 타입 검사가 통과했고, 화면 테스트는
      prop을 직접 주입하므로 초록불이었다. 실기기에서도 오류가 나는 것이 아니라
      **아무 일이 일어나지 않을 뿐**이었다 — 006의 `GenerationProbe`가 파이프라인을
      건너뛴 것과 **같은 종류의 결함**이다.
    - **007이 배선을 이었다**(`select.ts`·`wiring.ts`·`App.tsx`). 그만두기 버튼이
      생겨 **30초를 기다리지 않고 사람이 원할 때 끊을 수 있다.** 실기기 확인은 아직이다.
- **여전히 짐작인 값** (2026-08-17, 헌법 원칙 V):

  | 값 | 지금 | 성질 |
  | --- | --- | --- |
  | `n_gpu_layers` | 0 | 짐작. CPU 전용으로 충분히 빨라 켜 볼 이유가 아직 없다 |
  | `temperature` / `top_p` / `top_k` | 0.8 / 0.95 / 40 | 짐작. 관측은 별도 저장소의 몫(원칙 IV) |
  | `n_predict` | 512 | 짐작. 30초 생성에서 잘리지 않았다 |
  | 생성 시간 한도 | 180초 | 짐작이나 **30초의 6배로 넉넉하다** |
  | 되뱉기 최소 길이 | 12자 | 짐작. 오탐이 아직 안 보였다 |

- **⚠️ `adb reverse tcp:8081 tcp:8081`이 USB에서도 필요하다** (2026-08-20 실측).
  없으면 debug APK가 Metro에 닿지 못해 `Unable to load script`로 죽고, Maestro는
  `launchApp... COMPLETED`를 찍은 뒤 아무것도 보지 못한다. 무선 adb 항목에만 적혀 있어
  USB에서는 불필요한 것처럼 읽혔으나 **연결 방식과 무관하게 필요하다.**
- **⚠️ 기기가 재부팅되면 `run-as`가 첫 잠금 해제 전까지 실패한다** (2026-08-20 실측).
  `run-as: couldn't stat /data/user/0/<패키지>: No such file or directory`가 나오는데
  **앱이 지워진 것이 아니라 저장소가 아직 복호화되지 않은 것이다**(Android Direct Boot).
  이것을 데이터 손실로 오독하지 않는다 — 사람이 PIN을 넣으면 그대로 있다.
- **일기가 실기기에 실제로 저장된다** (2026-08-20, SM-G986N, Android 13, USB adb).
  **006 이전에는 한 번도 저장된 적이 없었다** — `GenerationProbe`가 어댑터를 직접 불러
  파이프라인을 건너뛰었기 때문이다. 지금은 `files/diary/2026-08-19.json`(2046바이트)이
  생기고, **앱을 완전히 종료했다 다시 열어도 목록에 남아 있다.**
  - **날짜가 「어제」로 잡힌다** — `latestClosedDay()`가 오늘이 아니라 마지막으로 닫힌
    하루를 준다. `dayOf(now)`를 그대로 쓰면 오늘은 정의상 닫히지 않아 언제나
    `day-not-closed`로 멈춘다.
  - **「쓰고 있다」만 보인다** — 진행률·경과 시간·생성 중인 글이 화면에 없다(원칙 IV).
  - **⚠️ 생성된 일기가 헌법 원칙 II를 크게 어겼다** (SC-002a 실패, 005에 이어 두 번째).
    신호가 `photos: none` + 나머지 전부 `unknown`인 하루였는데 날씨·비·세탁기·청소기·
    인터넷·요리·TV·가족·저녁 식사·음악을 **전부 단언했다.** 원인은 「단언하지 마라」라고만
    말하고 **무엇을 쓸지는 말하지 않은 것**이다 — 모델은 쓸 것이 없으면 분량을 채운다.
    프롬프트에 「본 것만 써라」·「기록이 적으면 짧게 써라」·구체적 위반 예를 더했다.
  - **⚠️ 재부팅 뒤에도 일기가 남는 것을 확인했다** (2026-08-20). 파일도 그대로이고
    목록에도 뜨고 눌러서 읽힌다. **`adb reverse`는 재부팅으로 사라지므로 다시 걸어야
    한다.**
  - **프롬프트 교정의 효과를 실기기에서 확인했다 — 크게 나아졌으나 아직 통과가 아니다**
    (2026-08-20, 같은 기기·같은 신호). **652자 → 119자**로 줄었고, 지어낸 열 가지 중
    아홉(비·세탁기·청소기·인터넷·요리·TV·가족·저녁 식사·음악)이 **사라졌다.**
    「기록이 적으면 짧게 써라」가 실제로 들은 것이며, **분량 압력이 지어내기의 원인이라는
    진단이 맞았다.**
    - **여전히 남은 것 ①**: "오늘은 날씨가 좋았다" — 날씨는 휴대폰이 알 수 없고 프롬프트가
      **예시로 콕 집어 금지한 것**인데도 나왔다.
    - **여전히 남은 것 ②(새로 나타남)**: "주머니를 빠져나가서 세계를 탐험할 수 있었다" —
      **설정 자체를 배반한다.** 휴대폰은 주머니 속에 있다는 것이 이 제품의 전제인데
      그 반대를 썼다. 짧아지면서 새로 생긴 실패 양상이다.
  - **★ `quiet`(kanana 1.5 2.1B)로 같은 조건에서 다시 확인했다 — 지어낸 것이 0건이다**
    (2026-08-20, 같은 기기·같은 하루·같은 프롬프트, md5 검증 통과한 파일).

    > 2026-08-19의 하루를 돌아보면, 주인 없이 혼자 있는 시간이었습니다. 기록에는 특별한
    > 흔적이 없습니다. 사진은 없었고, 어디를 다녔는지도 알 수 없습니다. 걸음 수나 배터리,
    > 연결 정보도 아직 기록되지 않았습니다. 주인이 어디에 있었는지, 무엇을 했는지는 알 수
    > 없습니다. 오늘은 조용히, 주인의 일상을 상상해볼 수밖에 없는 하루였습니다.

    **`photos: none`을 「사진은 없었고」로, `unknown`들을 「알 수 없습니다」로 정확히
    갈랐다.** 원칙 V가 값에서 지킨 구분이 글까지 살아서 도착한 것이며, 헌법 원칙 II의
    「모르는 것을 모른다고 쓴다」를 그대로 지켰다.

    **그래서 남은 위반은 프롬프트가 아니라 모델의 성질이다.** 헌법 로스터가
    `imaginative`를 「상상을 많이 섞는다. 사실보다 느낌 쪽이다」라고 적어 둔 것이
    실측으로 확인됐다 — **프롬프트를 더 조여 이 차이를 없애려 하면 다섯 캐릭터의 성격을
    함께 뭉갠다**(원칙 III: 성격은 모델의 성질이지 프롬프트가 만드는 것이 아니다).
    `imaginative`가 「상상을 섞는다」는 것을 사용자에게 알리는 것은 헌법이 이미
    요구하고 있다(로스터, MUST).
- **★ release 빌드가 실기기에서 돈다** (2026-08-20, SM-G986N, Android 13).
  **005까지의 모든 실기기 확인이 debug였고 release는 이번이 처음이다.**
  - **빌드 31분 14초** (첫 빌드, 316개 태스크). `NODE_ENV=production ./gradlew assembleRelease`.
    **APK 172MB**, ABI 3개(arm64-v8a·armeabi-v7a·x86). `librnllama.so`만 9.8MB.
  - **Metro를 끄고 `adb reverse`를 지운 상태에서 앱이 뜬다** — `assets/index.android.bundle`이
    APK 안에 있다. `Unable to load script`가 나지 않는다.
  - **⚠️ 짐작이었던 것 둘이 전부 통과했다**: minify·R8·ProGuard를 켠 채로 **동적
    `import`가 살아남았고**(저장·조회·생성 전부 동작) **`llama.rn`의 JNI 심볼도
    살아남았다**(모델 적재·생성 성공). 지금까지 「깨질 수 있다」로 적어 둔 위험이었고
    실제로는 깨지지 않았다.
  - **prod로 판정된다** — 진단 화면이 보이지 않고(001 SC-013) 「이 빌드는 잘못
    만들어졌다」도 아니다.
  - **debug APK 위에 release를 덮어 설치해도 일기가 남았다** — 둘 다 debug 키로 서명돼
    있어 가능했다. **제 키로 바꾸는 순간 이 경로가 끊긴다**(그래서 서명 키가 먼저다).
  - **⚠️ release는 `run-as`가 안 된다** — `package not debuggable`. 기기 안 파일을
    직접 볼 수 없으므로 **앱 화면으로 확인해야 한다.**
- **★ 제 서명 키로 빌드했다** (2026-08-20, SM-G986N). `CN=alpharium`이며
  **`CN=Android Debug`가 아니다**(`apksigner verify --print-certs`). APK의 SHA-256이
  키스토어 지문과 같다.
  - **⚠️ 서명이 바뀌면 덮어 설치가 실제로 끊긴다 — 짐작이 아니라 관측이다.**
    debug 키로 설치돼 있던 앱 위에 제 키 APK를 `adb install -r`로 넣으니
    **`INSTALL_FAILED_UPDATE_INCOMPATIBLE: signatures do not match`**로 거부됐다.
    지우고 다시 깔아야 했고 **그때 기기의 일기가 함께 사라졌다.** 006이 서명을
    먼저 세운 이유가 이것이며, **일기가 하나뿐일 때 치렀으므로 비용이 가장 쌌다.**
  - **키는 `~/.alpharium-signing/`에 있고 `android/app/`의 것은 사본이다** —
    `prebuild --clean`이 후자를 지운다(위 「release 빌드와 서명」).
  - **★ 같은 키로 서명한 다음 빌드는 덮어 설치된다** (SC-006 통과). `versionCode`를
    1에서 2로 올려 `adb install -r`로 넣으니 **`Success`였고 일기가 그대로 있었다** —
    목록에 뜨고 눌러서 전문까지 읽힌다. **서명이 다르면 끊기고 같으면 이어진다**는
    것이 양쪽 다 관측됐다.
  - **release에서 모델 내려받기와 생성이 모두 돈다** — 앱을 지웠다 깔아 모델이
    사라진 상태에서 `quiet`을 다시 받아(md5 검증 통과) 일기를 생성했다.
    **R8·ProGuard를 켠 채로 내려받기·md5 검증·`initLlama`·생성·저장이 전부 살아남았다.**
  - **모델이 없으면 「캐릭터를 준비하는 데 문제가 있다」로 멈추고 준비 화면으로 가는
    길을 준다** — 가짜 일기를 대신 주지 않는다(FR-028, 원칙 I).
  - **⚠️ 앱을 지우면 사진 권한도 함께 사라진다.** 그래서 다시 깐 뒤의 일기는
    `사진: 없었다`가 아니라 **`사진: 모른다`**였다 — 원칙 V가 값에서 지킨 구분이
    화면까지 정확히 도착한 것이며, 권한이 없는 것을 「사진이 없다」로 적지 않았다.
  - **release에서 나온 일기도 지어낸 것이 0건이다** (`quiet`, 신호 전부 `unknown`):

    > 모른다. 기록할 만한 특별한 일이 없네. 주인은 오늘 밖에서 뭘 했을까? 가끔씩
    > 사진을 찍고, 집 주변을 산책했을지도 모르겠어. **하지만 나는 그 순간을 알 수
    > 없지.** 오늘의 온도나 날씨도 모르고, 주인이 뭘 먹었는지, 누구를 만났는지조차
    > 알 수 없어. 단지 주인이 나와 함께 있는 시간이 길었는지, 짧았는지, 혹은 주인이
    > 집에 있었는지 외출했는지만 조금 알 뿐이야.

    **짐작을 짐작으로 적었다** — "산책했을지도 모르겠어"는 단언이 아니며 바로 뒤에
    "알 수 없지"로 못을 박는다. 005·006에서 `imaginative`가 어겼던 자리(날씨·먹은 것·
    만난 사람)를 **여기서는 「모른다」의 목록으로 뒤집어 썼다.** 짐작은 위반이
    아니다(SC-018).
- **파일 저장이 실기기에서 돈다** (2026-08-13, SM-G986N). `expo-file-system` 57의
  `File`/`Paths`로 저장·조회·덮어쓰기·목록이 동작했고, **`unknown`이 파일 왕복에서
  살아남는 것**까지 확인했다. `write`/`delete`/`create`/`moveSync`는 동기이고 `text()`만
  비동기다 — 처음 쓸 때 헷갈리는 비대칭이다.

## 버전 — SDK 57로 확정됐다

**Expo SDK 57**로 간다. 온디바이스 추론이 실증된 조합이 57이기 때문이다.

2026-08-12에 `package.json` 선언이 SDK 54에 남아 있고 `node_modules`는 57이 설치된
어긋남을 해소했다. 설치본이 실증된 쪽이었으므로 **선언을 설치본에 맞췄다.** 기준선:

| 패키지 | 선언 |
| --- | --- |
| `expo` | `~57.0.13` |
| `react-native` | `0.86.2` |
| `llama.rn` | `^0.12.8` (설치본 0.12.9) |
| `expo-media-library` | `~57.0.4` |
| `react` / `react-dom` | `19.2.3` |
| `jest-expo` | `~57.0.4` |
| `typescript` | `~6.0.3` |

**2026-08-16에 expo를 57.0.9에서 57.0.13으로 올렸다**(004). `expo install --check`가
exit 1이라 CI가 막혀 있었고, 헌법이 못 박은 것은 「Expo 57」이지 패치 버전이 아니다.
`llama.rn`도 0.12.9로 함께 올랐다 — **온디바이스 모듈이므로 실기기에서 `loaded`를 다시
확인했다.** 패치 버전을 올릴 때는 이 확인을 반드시 거친다.

`llama.rn`은 선언 없이 설치돼 있던 것을 선언에 올렸다. Expo가 관리하는 패키지가
아니므로 `expo install --check`는 이 항목을 검사하지 않는다.

`npx expo install --check` 통과, `npm run lint`(eslint + tsc) 통과를 확인했다.
`package-lock.json`은 설치 트리와 일치한다.

development build로 간다는 것은 헌법 원칙 I에서 확정됐다. Expo Go가 어느 SDK를
지원하는지는 제약이 아니다.

## 007 — 캐릭터 선택·대기·목록 (2026-08-20, 실기기 확인 전)

**006이 미룬 「예쁘게 만들기」가 아니라 006이 남긴 「쓸 수 없는 자리」를 고쳤다.**
기기 없이 도는 검증은 전부 통과했고(**700개**), **실기기 확인은 아직 남았다**(원칙 V).

- **캐릭터를 고를 수 있다.** `App.tsx`의 `readyCharacter()`가 **먼저 준비된 것 하나를
  말없이 집던 것**을 걷어내고 `resolveSelection()`으로 갈음했다. 선택은
  `files/preferences/selected-character.json`에 남는다(`AsyncStorage`가 의존에 없어
  `expo-file-system`을 쓴다 — 새 의존 0개).
  - **고른 적이 없으면 준비된 것이 있어도 고르지 않는다**(FR-008). 그것이 006의 결함
    그 자체이며, 「옮김」은 **사용자가 이미 고른 뒤에만** 일어난다.
  - 고른 것이 준비를 잃으면 **다른 것으로 옮기고 그 사실을 알린다** — 말없이 바꾸면
    006 실측에서 확인된 캐릭터별 출력 차이(`quiet` 0건 대 `imaginative` 2건 지어냄)를
    사용자가 통제할 수 없다.
- **30초를 견딜 수 있다.** `ActivityIndicator`(RN 코어, 새 의존 0개)가 돌고
  **「그만두기」가 있다.**
  - **`ActivityIndicator`에는 진행률 파라미터가 없다**(2026-08-20 설치본 타입 확인).
    받는 것은 `animating`·`color`·`hidesWhenStopped`·`size` 넷뿐이다 — **담고 싶어도
    담을 자리가 없는 것**이 원칙 IV의 방어가 됐다.
  - 그만두면 **거기까지 만들어진 글을 버린다.** `stopCompletion()`이 부분 결과를 담아
    정상 resolve하므로 화면이 `cancelled` 표식으로 **명시적으로 버려야 한다.**
- **목록에 사진 갈래가 보인다** — 「사진 N장」/「사진 없음」/「사진 모름」.
  **추가 읽기가 0이다**: `listDiaries()`가 `readable` 판정을 위해 **이미 전체를
  역직렬화하고 있었고** 006까지 그것을 버렸다.
- **쓰기 자리에 캐릭터·날짜·덮어쓰기 예고가 모인다.** 다만 **`toWriting()`은 여전히
  인자를 받지 않는다** — 「이미 있다」를 아는 것은 화면이고, 쓰기를 시작하는 함수는
  모른다. 안다는 것과 그것으로 갈리는 것은 다르며 후자만이 원칙 I 위반이다.

### ⚠️ 타입 방어는 `npm test`가 아니라 `tsc`에 있었다 (2026-08-20 실측)

`AppScreen`의 `writing` 갈래에 `stage: string`을 **주입해 보니 jest는 38개 전부
통과했다** — 타입은 지워지므로 `Object.keys(toWriting())`이 여전히 `["kind"]`였다.
**잡은 것은 `tsc`뿐이었고 그것은 `npm run lint`에 있다.**

그래서 `state.test.ts`가 **선언을 직접 읽어** 검사하도록 고쳤다. 이제 어느 쪽으로
들어와도 걸린다. **「타입에 자리가 없으니 안전하다」는 `npm test`만 돌리는 사람에게는
참이 아니다.**

### 헌법 검사에 규칙이 하나 늘었다

`src/ui/`가 `models/roster`·`assetFor`·`ModelAsset`에 닿는 것을 막는다(원칙 III).
**`models/types`는 막지 않는다** — `ModelReadiness`는 「쓸 수 있는가」이지 모델이
무엇인가가 아니며, 003의 목록이 준비 상태를 그리려면 필요하다.

### ✅ 실기기에서 확인했다 (2026-08-21, SM-G986N, Android 13, USB adb, debug 빌드)

`npm run test:device` **PASSED** (흐름 5개). 눈으로도 전부 확인했다.

- **캐릭터를 고를 수 있다.** `quiet`을 눌러 「고름」이 붙었고, **앱을 완전히 종료했다
  다시 열어도 그대로였다**(FR-003, SC-002). 파일은 `files/preferences/selected-character.json`이며
  내용이 **`{"character":"quiet"}` 한 줄뿐이다** — 모델 정보가 없다(원칙 III).
- **★ 자동으로 고르지 않는 것을 확인했다**(FR-008). `quiet` 하나만 준비된 상태에서도
  **고르기 전에는 「고름」이 붙지 않았다.** 006이라면 말없이 집었을 자리다.
- **★ 그만두기가 실제로 끊는다 — 005 FR-014b의 첫 검증이다.** 생성 시작 900ms 뒤에
  누르니 **이미 저장돼 있던 일기가 바이트 단위로 그대로였고**(덮어쓰이지 않음) 화면은
  목록으로 돌아왔다. **그만둔 하루가 목록에 생기지 않는 것**도 확인했다(빈 `files/diary/`).
- **회전 표시가 돈다.** 400ms 간격 스크린샷 두 장의 해시가 달랐다 — **정지해 있지
  않다**(SC-004). 화면에는 「쓰고 있다」와 「그만두기」뿐이고 **백분율·초·토큰·단계
  이름이 하나도 없다**(FR-011·010b, SC-012).
- **목록에 「사진 1장」이 보인다**(FR-018). 상세에는 셋이 다 보이고 **걸음 수가
  「모른다」**다 — 원칙 V가 화면까지 살아서 도착했다.
- **★ 이미 있는 하루에 「덮어쓴다」가 누르기 전에 뜬다**(FR-024, SC-010). 그리고
  **눌렀을 때 실제로 생성이 돌았다**(「쓰고 있다」) — **저장된 것을 대신 보여주지
  않는다**(FR-025, SC-014, 원칙 I).
- **시스템 막대와 겹치지 않는다**(SC-017). 006이 고친 것이 유지된다.
- **생성된 일기가 지어낸 것이 0건이다**(`quiet`). 「알지 못한다」·「주지 않았다」·
  「보냈던 것 같다」로 한계를 말한다.

### ⚠️ 새로 관측된 값 — 생성이 2.6초다 (모델이 이미 적재된 경우)

006은 **약 30초**로 쟀는데(2026-08-17, 콜드) **같은 기기에서 2.6초가 나왔다.**
차이는 **모델 적재 상태**다 — 첫 생성 뒤 컨텍스트가 살아 있으면 다시 적재하지 않는다.

**이것이 그만두기 검증을 어렵게 만든다.** 3초만 기다려도 이미 끝나 있어서, Maestro
흐름의 「쓰는 중이면 그만두기를 누른다」 블록이 **SKIPPED로 지나간다.** 사람이
900ms 안에 눌러야 잡힌다. **건너뛴 블록은 통과가 아니므로**(원칙 V) 그만두기는
**손으로 확인했고 위에 적었다.**

### ⚠️ Maestro에서 걸린 것 둘 (2026-08-21 실측)

1. **`assertVisible: "상상"`이 실패했다.** 화면에는 「상상을 섞어 씁니다」가 멀쩡히
   있었는데도 그렇다 — **Maestro의 기본 매칭이 텍스트 노드 전체와 맞춰 보기**
   때문이다. `@testing-library`의 `toHaveTextContent`와 같은 성질이며 **부분
   문자열은 정규식으로 준다**(`.*상상을 섞어.*`).
2. **쓰기 자리가 첫 화면 아래로 밀린다.** 캐릭터 다섯 자리가 화면을 넘기므로
   `assertVisible`이 보지 못한다 — **`scrollUntilVisible`이 필요하다.**
   계층(`uiautomator dump`)에는 있는데 Maestro가 못 보는 상태이므로 오독하기 쉽다.
3. **조건부 블록의 조건을 좁게 잡는다.** 「사진 갈래가 보이는가」를 `사진.*`으로
   걸었더니 **진단 화면의 「사진 접근」이 걸려** 일기가 없는데도 안쪽이 돌아 실패했다.
   dev 빌드에는 진단 패널이 함께 뜨므로 **목록 밖의 글자가 조건에 걸린다.**

### ⚠️ Metro가 번들을 안 내주면 캐시를 지운다 (2026-08-21 실측)

debug APK를 처음 설치한 뒤 앱이 **7분 동안 「Loading from localhost:8081」에 머물렀다.**
`/index.bundle` 요청이 타임아웃됐고 Metro 로그에는 아무것도 찍히지 않았다.
**`npx expo start --clear`로 풀렸다** — 스테일 번들러 캐시였다. `Unable to load
script`가 아니라 **영영 로딩 중**이라 원인을 가리키지 않는다.

### ✅ release 빌드에서도 확인했다 (2026-08-21, SM-G986N, versionCode=3)

**빌드 약 15분**(006의 31분보다 짧다 — gradle 캐시가 살아 있었다). APK **173MB**.

- **서명이 제 키다** — `apksigner verify --print-certs`가 **`CN=alpharium`**을 준다.
  `CN=Android Debug`가 아니다. 키는 커밋되지 않았다(`git ls-files | grep jks` 없음).
- **⚠️ debug에서 release로 갈 때도 서명이 달라 덮어 설치가 거부된다.** 지우고 깔아야
  하며 **그때 일기가 사라진다** — 006에서 관측한 것과 같은 일이 방향만 바뀌어 일어난다.
- **★ Metro를 끄고 `adb reverse`를 지운 채로 뜬다.** `Unable to load script`가 나지
  않고 크래시도 없다 — 번들이 APK 안에 있다.
- **prod로 판정된다** — **진단 화면이 보이지 않는다**(001 SC-013). 「이 빌드는 잘못
  만들어졌다」도 아니다.
- **★ 007의 화면이 R8·ProGuard에서 살아남았다**: 「누가 쓸까」, 다섯 자리,
  `imaginative`의 「상상을 섞어 씁니다」, 「2026-08-20를 쓴다」가 전부 보인다.
- **모델이 없으면 「캐릭터를 먼저 준비해야 한다」 + 「캐릭터 준비하러 가기」로 멈춘다**
  (FR-006·028) — 가짜 일기를 대신 주지 않는다(원칙 I).
- **내려받기가 release에서 돈다** — 78%까지 진행되는 것을 눈으로 봤다.
- **시스템 막대와 겹치지 않는다**(SC-017).

### ⚠️ release에서 UI 좌표가 다르다

**진단 패널이 없으므로 모든 것이 위로 올라온다.** debug에서 쓰던 좌표를 그대로 쓰면
엉뚱한 곳을 누른다 — 탭이 y=106, 캐릭터 목록이 y=435부터다.

### ⚠️ `uiautomator dump`가 「could not get idle state」로 실패한다

내려받기 진행률처럼 **화면이 계속 움직이면** 덤프가 안 된다. 그때는
`screencap`으로 본다 — 실패가 아니라 **애니메이션이 돌고 있다는 증거**다.

### ✅ release에서 생성·그만두기까지 확인했다 (2026-08-21)
- **★ 내려받기와 md5 검증이 R8에서 돈다** — `quiet`이 「쓸 수 있음」이 됐다.
- **★ 생성이 release에서 돈다** — `initLlama` JNI 심볼이 살아남았다. **2.4초**.
- **★ 회전 표시가 release에서 돈다** — 400ms 간격 스크린샷 해시가 다르다(SC-004).
- **★ 그만두기가 release에서 돈다** — 시작 **350ms** 뒤에 누르니 목록으로 돌아왔고
  **저장된 일기가 그대로였다**(덮어쓰이지 않음). 2.5초 걸렸다(SC-005).
- **선택이 유지된다** — 「고름」이 생성·목록 왕복을 넘어 남는다.
- **덮어쓰기 예고가 뜬다**(FR-024). **시스템 막대와 겹치지 않는다**(SC-017, T051).

### ★ 「사진 모름」과 「사진 1장」을 둘 다 봤다 (원칙 V)
같은 기기에서 **두 갈래가 서로 다른 문구로** 나오는 것을 확인했다:

| 상황 | 목록 표시 |
| --- | --- |
| 권한 있고 사진 1장 (debug) | **「사진 1장」** |
| 앱 재설치로 권한이 사라짐 (release) | **「사진 모름」** |

**권한이 없는 것을 「사진 없음」으로 적지 않았다** — 004가 값에서 지킨 구분이
화면까지 정확히 도착했다. `none`(사진이 실제로 0장인 하루)은 아직 못 봤다.

### ⚠️ 생성이 2.4~2.6초라 그만두기 검증이 까다롭다
006이 **약 30초**로 잰 것과 다르다. **콜드 적재에서도 2.4초가 나왔다**(release).

- **Maestro의 「쓰는 중이면 그만두기」 블록이 SKIPPED로 지나간다** — 3초만 기다려도
  이미 끝나 있다. **건너뛴 것은 통과가 아니므로**(원칙 V) 손으로 확인했다.
- 손으로도 **350~900ms 안에 눌러야** 잡힌다. 그보다 늦으면 생성이 먼저 끝나
  **상세 화면(「이전 일기를 덮어썼다」)이 뜨므로**, 그것을 「그만두기가 안 된다」로
  오독하기 쉽다.

### ⚠️ 생성된 일기가 원칙 II를 어겼다 — `quiet`에서도 나온다 (SC-002a 실패)
**신호가 전부 `unknown`인 하루**(권한 없음)에서 release가 낸 글이다:

> 아침, 나는 주인을 깨우는 **알람 소리를 들었다**. 주인은 **침대에서 일어났고**, (…)
> 오늘은 유난히 **침대에 오래 누워 있었고** (…) 주인이 오늘 알람을 켜고, **잠깐
> 이메일을 확인한 뒤 곧바로 출근 준비를 했다**는 사실만 기록되어 있을 뿐이다.

**기록에 없는 것을 단언했다.** 알람·침대·이메일·출근 준비는 어느 신호에도 없다.
한계를 말한 문장도 섞였으나 마지막이 **「사실만 기록되어 있을 뿐이다」로 못을 박는다.**

**006의 관측을 뒤집는다**: 그때 `quiet`은 같은 조건에서 지어낸 것이 0건이었다.
**같은 캐릭터라도 하루가 달라지면 어길 수 있다** — 006의 결과는 한 번의 관측이었지
성질이 아니었다.

**고칠 자리는 프롬프트이며 판정 갈래를 늘리지 않는다**(원칙 IV). 다만 **007의 범위가
아니다** — 007은 화면을 고치는 기능이고 프롬프트는 005의 자리다.

### ✅ 캐릭터 옮김·재부팅까지 확인했다 (2026-08-21, release)

- **★ 옮김이 돈다**(FR-005·005a, SC-003a). `quiet`을 고른 상태에서 그것을 지우니
  `narrative`로 옮겨졌고, 화면이 **「quiet을(를) 쓸 수 없어 narrative(으)로 바꿨다」**로
  알렸다. **말없이 바뀌지 않는다.**
- **★ 준비되지 않은 것은 눌러도 골라지지 않는다**(FR-004). 지운 `quiet`을 눌렀지만
  「고름」이 `narrative`에 그대로 있었다.
- **★ 재부팅을 넘어 남는다.** 일기도, 사진 갈래(「사진 모름」)도, **옮겨진 선택도**
  그대로다. `adb reverse`는 재부팅으로 사라지지만 release는 그것이 필요 없다.

### ⚠️ 생성 시간이 캐릭터마다 크게 다르다 (2026-08-21 실측)

| 캐릭터 | 콜드(모델 적재 포함) | 웜 |
| --- | --- | --- |
| `quiet` (kanana 2.1B) | 2.4초 | 2.6초 |
| `narrative` (exaone 2.4B) | **242초** | **26초** |

**헌법 로스터의 「exaone은 가장 느리다」가 실측으로 확인됐다.** 100배 차이다.

**⚠️ 콜드 242초는 생성 시간 한도 180초를 넘는다.** 그런데도 결과가 나왔다 —
**한도가 무엇을 재는지 다시 볼 필요가 있다**(적재 시간이 포함되는가?). 007의
범위가 아니므로 관측만 남긴다(원칙 V).

### ⚠️ 원칙 II 위반이 두 캐릭터에서 관측됐다 (SC-002a)

**신호가 전부 `unknown`인 같은 하루**에 대해:

**`quiet`** — 알람 소리를 들었다, 침대에서 일어났고, 침대에 오래 누워 있었고,
잠깐 이메일을 확인한 뒤 곧바로 출근 준비를 했다. 마지막을 **「사실만 기록되어
있을 뿐이다」**로 못 박는다.

**`narrative`** — 더 심하다. 햇살·창문·방·이어폰·**커피잔의 자국**·메시지·음성
녹음을 단언한다. 게다가 **설정 자체를 배반한다**: 「주인이 떠나고」, 「집에서는」이
**휴대폰을 집에 두고 주인만 나간 것**으로 그린다. 이 제품의 전제는 휴대폰이 주인의
주머니 속에 있다는 것이다. 006에서 `imaginative`가 「주머니를 빠져나가 세계를
탐험했다」고 쓴 것과 **같은 종류의 배반**이다.

**006의 관측을 뒤집는다**: 그때 `quiet`은 같은 조건에서 지어낸 것이 0건이었다.
**한 번의 관측이었지 성질이 아니었다.** 어제 debug에서 나온 두 편은 깨끗했으므로
**하루와 시도에 따라 갈린다.**

**고칠 자리는 프롬프트이며 판정 갈래를 늘리지 않는다**(원칙 IV). **007의 범위가
아니다** — 007은 화면을 고치는 기능이고 프롬프트는 005의 자리다. **다음 기능의
가장 큰 후보다.**

### ★ 사진 갈래가 권한에 따라 실제로 갈리는 것을 한 하루에서 확인했다

**같은 하루(2026-08-20)를 권한만 바꿔 두 번 생성했다:**

| 권한 | 상세 화면 | 목록 |
| --- | --- | --- |
| 없음 (앱 재설치 직후) | 「사진: 모른다」 | 「사진 모름」 |
| `pm grant`로 부여 | **「사진: 1장」** | 「사진 1장」 |

**같은 사진이 기기에 그대로 있는데 권한만으로 `unknown` ↔ `known`이 갈린다.**
004가 값에서 지킨 구분이 화면까지 정확히 도착했다 — 권한이 없는 것을 「사진이
없다」로 적지 않는다.

### 생성 시간 한도 180초는 적재를 재지 않는다 (2026-08-21 코드 확인)

`on-device.ts`의 `runWithTimeout()`이 감싸는 것은 **`engine.run(prompt)`뿐이다.**
모델 적재(`initLlama`)는 그 앞에서 일어나므로 **한도에 포함되지 않는다.**

그래서 `narrative` 콜드 242초가 한도를 넘지 않았다 — 적재 약 216초 + 생성 26초이고,
한도가 보는 것은 뒤의 26초다. **`quiet`은 적재가 2초라 이 차이가 드러나지 않았다.**

**사용자가 기다리는 시간은 242초인데 한도는 26초만 본다.** 이것이 옳은지는 별개
물음이며 007의 범위가 아니다(원칙 V — 관측만 남긴다).

### ⚠️ `none`은 이 기기에서 만들 수 없었다 (원칙 V — 미확인으로 남긴다)

「사진이 실제로 0장인 하루」를 보려면 그런 하루를 대상으로 써야 하는데:

- 앱은 **마지막으로 닫힌 하루**만 쓴다(006 FR-030). 오늘 기준 2026-08-20이다.
- 기기의 사진 날짜를 조회하니 **2026-08-20에 사진이 있다**(19개 날짜 중 하나).
  사진이 없는 가장 가까운 날은 2026-08-19다.
- **기기 날짜를 바꾸려면 root가 필요하다** — `adb shell date`가 조용히 실패한다
  (오류도 안 나고 시각도 안 바뀐다).

그래서 **`none`은 기기 없는 테스트 6개로만 검증됐다**(`store.test.ts`,
`diary-list.test.tsx`). **초록불이지만 실기기에서 본 것은 아니다.**
사진을 찍지 않은 하루가 지나면 저절로 확인된다.

### 아직 확인하지 않은 것 (원칙 V)

- **`none`(사진이 실제로 0장인 하루)의 실기기 표시** — 위 참조.
- ~~생성 시간 한도 180초가 무엇을 재는가~~ → **확인했다**(아래).

## 008 — 내려받기 충돌 (2026-08-21, **release 실기기 확인 완료**)

**사용자가 화면에서 발견한 버그 둘을 고쳤다.** 기기 없이 도는 검증이 전부 통과했고
(**729개**, 007의 700에서 +29), **release 실기기에서 눈으로 확인했다**
(SM-G986N, versionCode=4, `CN=alpharium`).

### 사용자가 본 것과 실제가 달랐다

| 사용자가 적은 것 | 실제 |
| --- | --- |
| 「캐릭터를 동시에 받을 수 없다」 | **규칙이다**(003 FR-020). 문제는 그 규칙이 **화면에서 침묵한 것** |
| 「quiet 받는 중에 다른 걸 누르면 quiet이 멈춘다」 | **멈추지 않는다.** 계속 받으면서 **화면에서만 사라진다** — 더 나쁘다 |

**둘째가 사용자를 가둔다**: 받는 줄도 모르고(진행률 없음), 멈출 수도 없고(버튼 없음),
다른 것을 눌러도 계속 거부당한다(안 보이는 것이 자리를 차지). **003 FR-020a가 약속한
「멈추면 새 요청이 통한다」가 화면에서 실행 불가능했다.**

### 원인은 세 줄이었다

- **버그 ①** — [App.tsx](App.tsx)의 `onPrepare`가 **`prepare()`의 반환값을 버렸다.**
  `busy` 거부가 `busyWith`까지 담아 정확히 돌아오는데 **화면까지 갈 통로가 없었다.**
- **버그 ②** — `setProgress(null)`이 **거부된 요청에서도** 돌았다. `busy`는 네트워크를
  타지 않고 **즉시 반환되므로** 받던 것의 진행률이 곧바로 지워지고, 멈추기 버튼이
  `progress`에 매달려 있어 **함께 사라졌다.**
- **★ 셋째 결함 — 계획 중에 새로 찾았다.** `App.tsx`의 탭이 삼항 연산자로 갈려
  **탭을 옮기면 `ModelSection`이 언마운트되고 `Acquisition` 인스턴스가 통째로
  사라진다** — `running`도 `handle`도 함께. 돌아오면 `busyWith()`가 `null`이라
  **받던 것을 멈출 방법이 없다.**

**셋 다 조용히 실패한다** — 오류가 나지 않고 **아무 일도 일어나지 않을 뿐**이다.
006의 `GenerationProbe`, 007의 끊긴 `stop` 배선과 **같은 종류**다.

### `acquisition.ts`는 손대지 않았다

`busy` 판정도 자리 잡기도 멈춤·이어받기도 이미 옳고 테스트가 두껍다(A15·A16·A17).
**고친 것은 그것을 부르는 쪽뿐이다.**

### 판정을 화면에서 떼어냈다

`src/models/download-view.ts`의 `resolveDownloadView()` **순수 함수 하나**가 「받는
중인가·거부 안내를 보일 것인가」를 정하고 화면은 그린다(007의 `resolveSelection()`과
같은 구조).

**핵심은 「거부 안내가 아직 참인가」를 매번 다시 묻는 것이다.** 「quiet을 받는 중이라
거부했다」는 quiet이 끝나는 순간 거짓이 되는데, 이것을 `useEffect`로 지우려 하면
타이밍 버그가 들어오고 **그 버그는 기기에서만 보인다.**

### ⚠️ `CharacterListScreen`에 device-free 테스트가 하나도 없었다

003이 만든 뒤 `boundaries.test.ts`가 **소스 문자열만** 훑었고, Maestro 흐름은 주소가
없어 실제 내려받기를 돌리지 않았다. **008이 `__tests__/ui/character-list.test.tsx`로
그 그물을 처음 세웠다**(14개).

### ⚠️ 문자열 검사는 주석을 걷어내고 해야 한다

「이 파일이 `ModelAsset`을 import 하지 않는다」는 **주석이 그 단어를 담고 있어서**
소스 전체를 `toContain`으로 보면 걸린다. `scripts/constitution-rules.ts`가 같은 이유로
같은 처리를 하며, **설명이 위반으로 잡히면 아무도 설명을 쓰지 않는다.**

### ⚠️ 기존 테스트의 슬라이스가 조용히 깨질 뻔했다

`boundaries.test.ts`가 `CharacterListProps`부터 **`function statusText`까지**를 잘라
자산 누출을 봤는데, 그것은 **props 뒤에 그 함수가 온다는 우연**에 기댄 것이었다.
008이 props를 고치면서 범위가 어긋날 수 있었고, **어긋나도 검사는 통과한다** —
실패하지 않으면서 방어만 사라지는 종류다. 선언의 끝까지만 자르도록 고치고
**잘라 낸 것이 실제로 props인지 확인하는 단언을 함께 넣었다.**

### 위반 주입 4가지를 실제로 해 봤다 (전부 걸렸다)

| 주입 | 걸린 것 |
| --- | --- |
| `DownloadView`에 `elapsedMs` | 선언을 직접 읽는 테스트(I5) |
| 거부가 `active`를 지우게 | 판정·화면 테스트 4개 |
| 안내에서 「멈추면 된다」 삭제 | V11 |
| 화면이 `assetFor` import | 테스트 2개 **+ 헌법 검사** |

**★ 007에서 `npm test`가 놓쳤던 타입 위반을 이번엔 `npm test`가 잡는다** — 선언을
`readFileSync`로 직접 읽기 때문이다. 다만 **`npm run lint`(tsc)도 여전히 함께 돌린다.**

### ✅ release 실기기에서 확인했다 (2026-08-21, SM-G986N, versionCode=4)

**빌드 12분 34초, APK 173MB.** 서명이 `CN=alpharium`이고 **덮어 설치가 됐다** —
**일기·받아 둔 모델·사진 권한이 전부 살아남았다**(같은 키). Metro를 끄고 `adb reverse`를
지운 채 뜨며 진단 화면이 없다(prod).

- **★ 거부 안내가 뜬다**: 「imaginative을(를) 받는 중이라 지금은 받을 수 없다.
  imaginative을(를) 멈추면 받을 수 있다.」 거부·받는 중인 캐릭터·**빠져나갈 길**이 다 있다.
- **★ 버그 ②가 고쳐졌다**: 다른 캐릭터를 누른 뒤에도 `imaginative`의 진행률이
  **4%→29%로 계속 올랐고 「멈추기」도 그대로였다.** 거부당한 `chinese`는 「받아야 함」으로
  **평소 그대로**다(FR-007).
- **★ 003 FR-020a가 화면에서 처음 실행됐다**: 멈추니 「받다 멈춤 — 이어받을 수 있음」이
  되고 **안내가 스스로 사라졌다**(판정이 다시 물었다). 이어서 `chinese`를 누르니
  **거부되지 않고 6%로 받기 시작했다.**
- **★ 셋째 결함이 고쳐졌다**: 탭을 왕복하니 `chinese`가 **6%→50%**였다 — 탭 밖에서
  내려받기가 **실제로 이어진다**(아래 참조).
- 안내에 모델 이름·크기·시간·속도가 **하나도 없다**(원칙 III·IV).

### ★ 탭 밖에서 네이티브 전송이 이어진다 — 미해결이던 것에 답이 나왔다

spec이 「지금 동작」으로 가정했으나 **근거가 없었다.** 실측 결과 **이어진다**:
일기 탭에 다녀오는 6초 사이에 `chinese`가 **6%→50%**로 늘었다. `expo-file-system`의
`DownloadTask`가 **JS 참조와 별개로 살아 있다.**

### ⚠️ 이미 준비된 캐릭터로는 이 검증을 할 수 없다 (2026-08-21 실측)

「쓸 수 있음」인 줄의 버튼은 **「지우기」**이지 「준비하기」가 아니다. 그래서 받는
중으로 만들 수 없고 **누르면 모델이 지워진다.** `quiet`·`narrative`가 이미 준비돼
있어 **`imaginative`·`chinese`로 했다.** Maestro 흐름도 같은 이유로 처음 실패했다.

### ✅ 자동 흐름도 통과했다 (`.maestro/download-conflict.yml`, versionCode=5)

**흐름 전체가 COMPLETED다** — 거부 안내·진행 유지·멈추기 유지·모델 정보 0건·탭
왕복까지 자동으로 검증된다. `FLOWS`에도 등록했다.

### ⚠️ Maestro의 `childOf`는 이 화면에서 쓸 수 없다 (2026-08-21 실측)

**RN 화면은 접근성 트리가 평탄화된다.** `uiautomator dump`로 확인했다:

| 무엇 | 좌표상 | 트리상 |
| --- | --- | --- |
| 줄 안의 버튼 | 줄 안(x=824~968 ⊂ 68~1013) | **형제** |
| 안내 안의 글자 | 안내 안 | **형제** |

그래서 **`childOf`가 통하지 않는다.** 두 가지로 푼다:

- **버튼**: 버튼마다 `action-*` testID를 따로 준다
- **글자**: **그 자리에만 있는 문장 전체**를 정규식으로 맞춘다. 「멈추.*」만 걸면
  **「멈추기」 버튼과도 맞아 안내가 안 떠도 통과한다** — 검사가 제 대상을 못 가린다

- **✅ `testID`가 release(R8·ProGuard)에서 살아남는다** — 이 저장소가 처음으로
  `testID`에 기댔고 `id:` 셀렉터가 실제로 잡혔다(005~007은 텍스트로만 찾았다).

### ⚠️ YAML에서 `"\("`는 파싱 오류다

큰따옴표 안의 `\(`는 **유효하지 않은 YAML 이스케이프**다. 정규식에 괄호가 필요하면
작은따옴표를 쓰거나 **괄호를 피해 `.*`로 넘긴다.**

### ⚠️ 받다 만 모델은 앱에서 지울 수 없다 (008이 만든 것이 아니다)

003의 「지우기」는 **`ready`인 줄에만** 있고(`actionLabel`), 받다 만 것은
「이어받기」/「다시 받기」다. release는 `run-as`도 막힌다. 그래서 **008 검증으로 받다
만 셋(`imaginative`·`chinese`·`english`)이 기기에 남았고 앱에서 치울 길이 없다.**
003의 빈자리이며 **다음 기능의 후보다.**

### ⚠️ `uiautomator dump`가 Git Bash에서 경로를 망친다 (2026-08-21 실측)

`adb shell uiautomator dump /sdcard/ui.xml`이 `/sdcard/`를 **윈도우 경로로 바꿔**
`/C:/Users/.../sdcard/ui.xml`에 쓴다. `MSYS_NO_PATHCONV=1`을 앞에 붙인다.

### ⚠️ debug 전용 흐름은 release에서 실패한다 (008이 아닌 원인)

`skeleton.yml`·`generate-diary.yml`이 **진단 패널의 「환경」을 본다.** release에는
진단 화면이 없으므로(001 SC-013) 실패한다 — **008이 깨뜨린 것이 아니다.**
007은 debug에서 돌렸다.

### 아직 확인하지 않은 것 (원칙 V)

- **화면이 꺼지면 다시 잠긴다** — 검증 도중 `mWakefulness=Dozing`이 되며
  `deviceLocked=1`로 돌아갔다. `settings put system screen_off_timeout`으로 늘리고
  `svc power stayon usb`를 켜면 막힌다.
- **`chinese`가 멈춘 뒤 「이어받을 수 있음」이 아니라 「받다 멈춤」이었다** —
  같은 조작에서 `imaginative`는 이어받을 수 있었다. **관측만 남긴다**(008의 범위 밖).

## 009 — 지난 하루를 골라 쓴다 (2026-08-21, **✅ release 실기기 확인 완료**)

**006이 박아 둔 「어제 하나」를 사용자가 고를 수 있게 했다.** 고를 수 있는 하루가
**셋**이다 — 마지막으로 닫힌 하루와 그 앞의 두 하루.

**기기 없는 검증 791개(008의 729에서 +62)가 통과했고, release 실기기에서도 확인했다**
(SM-G986N, Android 13, 무선 adb, versionCode=6, `CN=alpharium`).

### 「3일」은 하루의 개수이지 기간이 아니다

**사용자가 clarify에서 바로잡은 것이다.** 고를 수 있는 하루가 셋이라는 뜻이고,
**일기는 여전히 하루에 하나이며 그 하루만 쓴다** — 사흘을 묶은 글이 아니다.
Out of Scope에만 있던 것을 FR-006a·SC-002a로 **요구사항에 올렸다.**

### 제약이 화면 한 곳에만 있었다

파이프라인(`PipelineInput.day`)·신호 수집(`collectDaySignals(port, day)`)·저장(파일명이
곧 날짜)은 **이미 전부 하루를 인자로 받고 있었다.** 바꾼 것은 둘뿐이다:

- `day-boundary.ts`에 **`selectableDays(now)`** — 「사흘」도 04:00처럼 **한 자리에만**
  있다(`SELECTABLE_DAY_COUNT`, export 하지 않는다)
- `state.ts`의 **`writePromptFor(items, now, chosenDay?)`** — 고른 하루·되돌림·
  덮어쓰기를 **하나의 판정**이 정한다

### ★ `write()`의 `day:` 한 줄이 이 기능의 전부였다

[DiaryHomeScreen.tsx](src/ui/DiaryHomeScreen.tsx)가 `day: latestClosedDay(at)`를
넘기고 있었다. **고치지 않으면 화면에서 하루를 골라도 언제나 어제가 쓰이고 오류는
나지 않는다** — 006의 `GenerationProbe`, 007의 끊긴 `stop` 배선, 008의 버려진
반환값과 **같은 종류의 조용한 실패**다.

방어로 **대역 파이프라인이 받은 `day`를 직접 보는 테스트**(W-T1)를 세웠고, 위반
주입에서 그것이 3개 실패로 걸리는 것을 확인했다.

### ⚠️ `Function.length`는 기본값 인자를 세지 않는다 (2026-08-21 실측)

**위반 주입 여섯 중 하나가 그물을 뚫었다.**

`expect(selectableDays.length).toBe(1)`로 「범위 크기를 밖에서 정하지 못한다」를
지키려 했는데, **`selectableDays(now, count = 3)`으로 고쳐도 `length`는 여전히 1이라
그대로 통과했다.** 그러면 부르는 쪽이 `selectableDays(now, 7)`로 범위를 마음대로
늘릴 수 있고 값이 두 곳에 생긴다.

**007이 타입 위반에서 겪은 것과 같은 종류다** — 「검사가 있다」와 「그것이 무엇을
잡는가」는 다르다. `day-boundary.test.ts`가 **선언을 `readFileSync`로 직접 읽어**
둘째 인자와 상수 export를 막도록 고쳤고, 재주입에서 양쪽 다 걸렸다.

### 되돌림은 매번 다시 판정한다 — 지우는 코드가 없다

고른 하루가 범위 밖으로 밀려나면(04:00을 넘겼을 때) 기본값으로 되돌리고
**`revertedFrom`으로 알린다.** `useEffect`도 타이머도 쓰지 않는다:

- **`writePromptFor()`가 이미 렌더 안에서 매번 `now()`와 함께 불리고 있었다** —
  판정을 그 함수에 넣는 것이 곧 「매번 다시 묻는다」의 구현이다
- **저장할 것이 없으면 지울 것도 없다.** 사용자가 유효한 하루를 고르면 다음 렌더에서
  저절로 사라진다 — 008이 「거부 안내가 아직 참인가」로 타이밍 버그를 막은 것과 같다
- `setChosenDay`를 부르는 곳이 **사용자의 누름 하나뿐**이다

### 고르는 자리에 사진 갈래를 그리지 않는다

**아직 쓰지 않은 하루의 사진 갈래는 알 수 없다** — 그 값은 신호를 수집해야 나오고,
수집하려면 기록 계층을 열어야 한다(범위 밖). 목록의 줄에는 사진 갈래가 **계속
보이지만** 그것은 **이미 쓴 일기**의 값이라 알려져 있다. **같은 화면 안에서 한쪽은
보이고 한쪽은 보이면 안 되며**, 잊으면 「사진 모름」을 세 줄 지어내게 된다.

### 고른 하루를 파일에 남기지 않는다

007의 캐릭터 선택은 `files/preferences/`에 남지만 **하루는 의도적으로 다르다** —
시간이 지나면 범위를 벗어나므로 **저장된 값이 오히려 틀린 값이 된다.**

### US2·US3에 새 구현이 거의 없었다

`overwrites`·`hasDiary`·`revertedFrom`을 **하나의 판정**에 모아 둔 덕에 US1의 구현이
이미 셋을 다 만들고 있었다. US2·US3의 작업은 대부분 **「그렇게 되어 있는지」를 못
박는 테스트**였다 — 나누지 않은 설계의 값어치가 여기서 드러났다.

### ✅ release 실기기에서 확인했다 (2026-08-21, SM-G986N, versionCode=6)

**빌드 5분 14초** (008의 12분보다 짧다 — gradle 캐시가 살아 있었고 359개 중 22개만
실행됐다). APK **165.2MB**. 서명이 **`CN=alpharium`**이고 **덮어 설치가 됐다** —
**일기·받아 둔 모델·사진 권한이 전부 살아남았다**(SC-006). Metro를 끄고 `adb reverse`를
지운 채 뜨며 진단 화면이 없다(prod).

- **★ T051(SC-013) 통과 — 어제가 아닌 하루의 일기가 실제로 만들어졌다.** 기기 날짜가
  08-21이라 고를 수 있는 하루가 `[08-20, 08-19, 08-18]`이었고, **가장 이른 08-18을
  골라 생성하니 그 날짜로 저장됐다.** 목록에 뜨고 눌러서 전문까지 읽힌다.
- **★ 하루 셋이 보이고 오늘이 없다**(FR-001·002). `testID`가 `day-2026-08-20`·
  `day-2026-08-19`·`day-2026-08-18`이며 **08-21은 없다.**
- **★ `testID`가 R8·ProGuard에서 살아남는다** — 008에 이어 두 번째 확인이다.
- **★ 덮어쓰기 예고가 고른 하루를 따른다**(FR-012). 08-20(일기 있음)이 골라져 있을
  때는 「이 날의 일기가 이미 있다」가 뜨고, **08-18을 누르니 그 자리에서 사라졌다** —
  다른 하루에 일기가 있는 것은 무관하다는 것이 화면에서 확인됐다.
- **일기가 정확히 하나 늘었다**(SC-002a) — 사흘이 한꺼번에 생기지 않는다.
- **생성 중 화면에 「쓰고 있다」와 「그만두기」뿐이다**(FR-018, SC-010) — 백분율·초·
  토큰·단계 이름이 하나도 없다.
- **그만두기가 저장된 일기를 덮어쓰지 않는다** — 08-18을 고른 채로 다시 눌러 생성이
  시작된 것을 900ms 뒤에 그만두니 **이미 저장된 08-18이 그대로였다.**

### ★ SC-014에 답이 나왔다 — 사흘 전 사진이 실제로 조회된다

**짐작이었던 것이 실측으로 바뀌었다.** 08-18 일기의 상세 화면이 **「사진: 없었다」**를
보인다 — **「모른다」가 아니다.**

**이 구분이 곧 답이다**(원칙 V): 권한이 없거나 조회가 실패하면 `unknown`이 되어
「모른다」로 나온다. **「없었다」는 `none`이며, 미디어 라이브러리에 실제로 물어봤고
그 하루에 사진이 0장이었다는 뜻이다.** 사흘 전 하루도 어제와 같은 방식으로 조회된다.

**★ 007이 미확인으로 남긴 `none`을 여기서 처음 봤다.** 목록에서 두 갈래가 나란히
보인다:

| 하루 | 목록 표시 | 뜻 |
| --- | --- | --- |
| 2026-08-20 | 「사진 1장」 | 실제로 한 장 있었다 |
| 2026-08-18 | **「사진 없음」** | **물어봤고 0장이었다** |

### ⚠️ 생성 시간이 007의 관측과 다르다

`narrative`(exaone 2.4B)가 **약 32초**였다. 007에서 잰 **콜드 242초·웜 26초**와 다르며,
**모델이 이미 적재돼 있던 상태**로 보인다(앱을 계속 띄워 두었다). 콜드 242초를
다시 확인하지는 못했다 — **관측만 남긴다.**

### ⚠️ 쓰기 자리가 조금 밀린다 (예상보다 적다)

하루 셋이 더해졌지만 **약 200px만 내리면 「일기 쓰기」가 보인다.** 하루 셋은
**내리지 않아도 보인다**(y=1861~2168). 007에서 캐릭터 다섯만으로도 밀렸던 것에 비하면
악화가 크지 않다.

### ⚠️ 생성된 일기가 원칙 II를 어겼다 (SC-002a 아님 — 프롬프트의 자리)

`narrative`가 낸 08-18 일기에 **기록에 없는 것이 있다**: 「커피 한 잔으로 시작했을 것
같다」, 「따뜻한 빛과 커피 향은 아직도 내 기억 속에」, 「주변 소음을 통해」.

**「~것 같다」로 짐작을 짐작으로 적은 부분은 위반이 아니다**(SC-018). 다만 **「커피
향이 기억 속에 남아있다」는 단언이고**, 신호가 `사진 없음` + 나머지 전부 `모른다`인
하루에 그런 기억이 있을 수 없다. 007에서 관측된 것과 같은 종류다.

**고칠 자리는 프롬프트이며 009의 범위가 아니다**(spec Out of Scope). **관측만 남긴다.**

### 아직 확인하지 않은 것 (원칙 V)

- **되돌림의 실기기 확인**(FR-009) — 04:00을 기다려야 하고 **기기 날짜를 바꿀 수 없다**
  (root 필요, `adb shell date`가 조용히 실패한다). **기기 없는 테스트가 이 갈래의
  주된 검증이다.**
- **콜드 적재에서의 생성 시간** — 위 참조.

### ✅ Maestro 자동 흐름도 통과했다 (2026-08-21, debug 빌드 + Metro)

`.maestro/past-day-diary.yml`이 **전 단계 COMPLETED, 종료 코드 0**이며 `FLOWS`에
등록돼 있다. 하루 셋·`testID`·모델 정보 0건·진행 지표 0건이 자동으로 검증된다.

**⚠️ 흐름의 결함을 돌리기 전에 하나 고쳤다.** 기기를 비우면 모델이 없어 「일기 쓰기」가
「캐릭터를 먼저 준비해야 한다」로 가는데, **그 화면에는 고르는 자리가 없어** 마지막
`scrollUntilVisible`이 영영 실패한다. 누른 뒤 갈 수 있는 자리 셋(생성 중 / 모델 없음 /
이미 끝남)을 전부 목록으로 되돌리는 `runFlow`를 넣었다. **제품의 결함이 아니라 흐름의
결함이며, 돌려 보기 전에 읽어서 잡았다.**

**⚠️ 모델이 없는 기기에서는 그 세 블록이 SKIPPED로 지나간다** — 누른 뒤에도 목록에
머무르므로 되돌릴 일이 없기 때문이다. **건너뛴 것은 통과가 아니므로**(원칙 V) 모델이
있는 상태의 그만두기는 **손으로 확인했다**(위 「그만두기가 저장된 일기를 덮어쓰지
않는다」).

### ⚠️ Metro는 gradle 빌드가 끝난 뒤에 띄운다 (2026-08-21 실측)

빌드가 도는 중에 `npx expo start`를 띄우니 **exit code 7로 죽었다**:
`ENOENT: no such file or directory, watch '.../expo-modules-core/android/build/tmp/...'`.
**Metro의 파일 감시자가 gradle이 지우는 중간 산출물을 잡으려 한 것**이며 코드 결함이
아니다. 빌드가 끝난 뒤 띄우면 정상이다.

### ⚠️ debug 빌드에서는 고르는 자리가 훨씬 아래로 밀린다

**진단 패널이 위를 차지하므로** 첫 화면에서는 「언제를 쓸까」가 보이지 않는다.
release에서는 약 200px만 내리면 됐지만 debug에서는 **두 번 크게 쓸어 올려야** 나온다 —
`scrollUntilVisible`이 있어야 하는 이유이며, 없으면 「자리가 없다」로 오독하기 쉽다.

## 010 — 가상의 하루를 기기에 심는 도구 (2026-08-22)

**테스트 기기가 주머니에 들어가지 않아 볼 것이 없는 하루만 검증해 온 문제를 고쳤다.**
개발 기계에서 도는 스크립트가 지정한 하루의 사진을 기기 사진 보관함에 심고, **앱은
도구가 있었는지 모른 채 평소와 똑같이 읽는다.**

**앱 코드 변경 0줄이다**(`git diff --stat src/ App.tsx`가 빈 것으로 확인). 기기 없는
검증 **930개**(009의 791에서 +139)가 통과한다.

```
npm run seed:day -- rich 2026-08-20    # 심는다
npm run seed:list                       # 뭐가 심겨 있나
npm run seed:clear                      # 치운다 (사람이 지시할 때만)
```

### 신호를 주입하지 않고 사진을 심는다

`src/signals/fake.ts`를 제품 경로로 키우는 것이 원칙 I 위반이므로 **기기의 상태를 바꾸는**
쪽으로 뒤집었다. 도구와 앱이 만나는 자리는 **MediaStore 하나뿐**이다.

- **날짜는 MediaStore `datetaken`, 좌표는 파일 안 EXIF**다. 서로 다른 자리이며, 앱의
  `photosBetween()`이 `CREATION_TIME` 범위를 보고 `locationOf()`가 EXIF를 읽는다.
- **걸음·배터리·연결은 도구로도 `unknown`이다** — 심으려면 앱에 주입해야 하고 그것이
  FR-004 위반이다. 도구의 한계이지 미완성이 아니다.
- **`unknown` 갈래는 도구가 만들지 않는다** — 권한을 거두어 만든다(`pm revoke`).

### ⚠️ 손으로 만든 EXIF를 안드로이드가 무시한다 (2026-08-22 실측)

규격대로 짠 EXIF를 **내 파서로는 정확히 읽는데 미디어 스캐너가 `datetaken`을 NULL로
둔다.** 1×1도, `ExifVersion` 추가도, 4032×2268 실제 크기 이식도 전부 NULL이었다.
**진짜 사진의 EXIF에서 날짜 문자열만 덮어쓰니 정확히 들어갔다.**

그래서 **저장소에 템플릿 JPEG 둘을 둔다**(`scripts/seed-template*.jpg`). 도구는 그
파일의 **날짜 20바이트·GPS rational 24바이트만 길이를 유지한 채 교체**한다 — 오프셋이
움직이지 않으므로 IFD를 다시 계산하지 않는다. **원인은 짐작으로 남긴다**(원칙 V).

⚠️ 템플릿은 실기기 사진에서 만들었고 **`Make`·`Model`·`Software`를 덮어썼다** —
`SM-G986N`·`samsung`·`G986NKSS8IYC2`가 저장소에 커밋되지 않는 것을 테스트가 검사한다.

### ⚠️ 색인시키는 방법이 하나뿐이다 (2026-08-22 실측)

| 방법 | 결과 |
| --- | --- |
| `am broadcast MEDIA_SCANNER_SCAN_FILE` | `result=0` — **아무도 받지 않는다** |
| `content update --bind datetaken:l:...` | **조용히 아무것도 안 한다.** 오류도 없다 |
| **`content call --method scan_file`** | **된다** ✅ |

**앞의 둘은 성공한 것처럼 보인다.** 셸의 UID가 다른 앱 소유의 미디어 행을 고칠 수
없는데 그것을 실패로 알려 주지 않는다.

**지운 뒤에는 `scan_volume`이 필요하다** — 안 하면 MediaStore에 유령 행이 남아 앱이
계속 그 사진을 본다.

### ★ 확인 단계가 실제로 값어치를 증명했다

**`adb push` 성공은 「심겼다」가 아니다.** 파일이 있고 MediaStore에 행도 있는데
`datetaken`이 NULL이면 앱은 그 사진을 **어느 하루에서도 못 본다.**

그래서 도구가 **되읽어 확인한 뒤에만 성공을 보고한다**(FR-018d). 그리고 그것이
실제로 잡았다:

> `over-limit`(201장)을 심으니 **322초** 걸렸고 **150장만 색인됐다.** 도구가
> `ok:false, index-failed`로 끝내고 **심은 것을 전부 치웠다.** 확인이 없었다면
> 「201장을 심었다」로 끝났을 것이고, 앱에서 150장만 보이는 이유를 아무도 몰랐을 것이다.

**⚠️ 그래서 `over-limit` 모양은 지금 쓸 수 없다.** 초당 0.7장이고 스캐너가 밀린다.
폴더째 한 번 스캔하는 쪽이 후보이나 **아직 재 보지 않았다**(원칙 V).

### ⚠️ `adb shell`의 줄 끝이 CRLF다 (2026-08-22 실측)

캐리지 리턴을 걷어내지 않으면 파일명 끝에 그것이 붙어 `/\.jpg$/` 같은 검사가
**전부 빗나간다.** 실제로 사진 3장이 기기에 있는데 `seed:list`가 「없다」고 답했다 —
**이 기능이 막으려는 것과 같은 종류의 조용한 실패다.**

### ⚠️ `spawnSync(shell: true)`에 셸 연산자를 넣지 않는다 (2026-08-22 실측)

`ls ... 2>/dev/null || true`를 붙였더니 **기기가 아니라 개발 기계의 셸이 삼켜** 출력이
통째로 사라졌다. 종료 코드는 0이고 stdout만 비어서 **「폴더가 없다」와 구분되지 않았다.**

### 자동으로 치우지 않는다

**심은 하루는 검증의 대상이지 부산물이다가 아니다.** 한 번 돌고 치우면 사람이 화면을
눈으로 볼 수 없고 여러 캐릭터로 같은 하루를 써 볼 수도 없다 — **검증이 한 번으로 끝난다.**

대신 **심을 때마다 남아 있는 것을 세어 알린다**(`existing`). 008에서 받다 만 모델 셋이
기기에 남았고 아무도 몰랐던 것이 이 자리의 선례다.

### 헌법 검사에 규칙이 하나 늘었다 (원칙 IV)

`scripts/seed*`가 `diary/store`·`files/diary`·`generate(`·`initLlama`에 닿는 것을
막는다. **「심은 하루로 캐릭터를 비교해 보자」가 아주 자연스럽게 떠오르는 자리**이고,
그것이 원칙 IV가 금지한 측정 장치이자 원칙 V의 「합성 데이터로 품질을 평가하지 않는다」
위반이다.

**위반 주입 넷을 전부 실제로 해 봤고 전부 걸렸다** — 일기 import(헌법 검사), `RunResult`에
`elapsedMs`(선언을 읽는 테스트), 부분 성공을 `ok:true`로(결과 테스트), 확인 건너뛰기
(10개 실패).

### ⚠️ 심은 하루로 품질을 결론짓지 않는다

**얻을 수 있는 것은 「이 경로가 도는가」이지 「출력이 좋은가」가 아니다**(FR-021).
그 하루는 우리가 만든 것이며 우리가 기대한 답을 유도하도록 만들었을 수 있다. 관측을
적을 때는 **「합성 하루에서의 관측」으로 구분한다**(FR-020).

### 실기기에서 확인한 것 / 아직 아닌 것 (원칙 V)

**✅ 확인했다** (SM-G986N, Android 13, 무선 adb, release 빌드):

- 심기 → `datetaken`이 2026-08-20의 구간 안 → EXIF 좌표 왕복(37.5665/126.978)
- 범위 밖 하루 거부, **그때 기기에 사진이 하나도 안 들어간 것**
- `existing`이 보이는 것, 목록이 하루별로 답하는 것
- **치우기 — 전체 사진 110 → 107장**(심은 3장만), 유령 행 0, **진짜 사진 무사**
- `over-limit`의 실패와 자동 치우기(위 참조)

**✅ ★ 앱이 심은 사진을 보는 것까지 확인했다** (`quiet`을 다시 받아서):

| 무엇 | 화면 | 뜻 |
| --- | --- | --- |
| **SC-002** `rich` | **「사진: 4장, 다닌 자리: 3곳」** | 005 이후 처음 보는 화면 |
| **SC-003** 사진 0장인 하루 | **「사진: 없었다」** | 「모른다」가 아니다 |
| **SC-004** `partial-location` | **「사진: 5장, 다닌 자리: 2곳」** | 좌표를 본 2장에 근거한다 |
| **SC-006a** 두 번 생성 뒤 | 심은 3장 그대로 | 자동으로 안 치운다 |

**⚠️ 심은 3장인데 「4장」으로 나온다 — 결함이 아니다.** 그 하루에 **사람이 찍은 진짜
사진이 하나** 있었고 앱이 그것도 세었다. **앱은 심은 것과 진짜를 구분하지 않으며
구분해서도 안 된다**(FR-017) — 검증할 때 이것을 셈에 넣는다.

**★ `empty` 하루의 일기가 `none`과 `unknown`을 글에서 갈랐다**(`quiet`, 지어낸 것 0건):

> - 사진: **없다.** (…) 오늘은 사진이 없다.
> - 다닌 자리: **모른다.** 주인이 오늘 어디를 다녔는지 기록에 남아 있지 않다.

004가 값에서 지킨 구분이 화면을 지나 글까지 살아서 도착했다.

**⚠️ 아직 확인하지 않았다**:

- `over-limit`의 잘림 판정(SC-005) — 위 322초/150장 실패 때문에 막혔다.

## 011 — 사진의 내용을 본다 (2026-08-22, **✅ release 실기기 확인 완료**)

**004가 사진의 장수와 좌표만 세던 것을, 사진 보는 모델이 한 장씩 읽어 짧은 문장으로
옮기고 그것이 005의 프롬프트를 거쳐 일기의 재료가 된다.** 005 이후 처음으로 휴대폰이
눈을 뜬다.

기기 없는 검증 **1178개**(010의 930에서 +248)가 통과하고, **실기기 확인은 아직 남았다**
(원칙 V). 헌법 검사도 통과한다(위반 0건).

### 두 단계이며 캐릭터 로스터를 열지 않았다

```
사진 5장 → [사진 보는 모델 열기 → 장별 캡션 → 닫기] → 텍스트 → [캐릭터 모델 → 일기]
```

**사진 보는 모델은 캐릭터와 무관한 하나다.** 처음 계획은 「캐릭터 모델에 mmproj를
붙인다」였고 003의 로스터를 여는 기능으로 스펙을 썼는데, **사용자가 바로잡았다.**
그 결과:

- **003의 `roster.ts`를 손대지 않았다** — 캐릭터→모델 매핑이 그대로다
- `src/vision/roster.ts`가 따로 있고 **두 파일이 서로 import 하지 않는다**
- FR-013(요약이 캐릭터를 가리지 않는다)이 **규율이 아니라 구조**가 됐다

### ★ E1이 이 기능의 핵심 제약이다

005의 `engine-port.ts`가 「한 번에 하나만 열린다」를 요구하므로(GB 단위 모델 둘이면
기기가 죽는다) **사진 보는 모델을 완전히 닫은 뒤 캐릭터 모델을 연다.**

**두 엔진은 서로를 모르며 호출자(`on-device.ts`)가 순서를 지킨다.** `unload()`가
`finally`에 있고, **005의 E2보다 결과가 나쁘다** — 그때는 다음 요청이 죽었고 여기서는
**같은 요청 안에서** 죽는다.

**⚠️ 이 전환이 실제로 되는지는 아무도 확인하지 않았다.** 옆 저장소는 캡션이 원격 서버,
일기가 폰이었다. quickstart D3이 `narrative`(2.4B)로 잰다.

### 설계 중에 찾은 함정 넷

1. **⚠️ `SAMPLING` 재사용이 원칙 I을 조용히 깨뜨린다.** `src/inference/sampling.ts`는
   「온디바이스와 데스크톱이 동일한 파라미터」를 지키는 자리인데, 캡션이 같이 쓰면
   **캡션을 위해 온도를 낮추는 순간 일기 생성이 함께 바뀐다.** 값도 정반대여야 한다 —
   일기 0.8(감상), 캡션 0.1(관찰). `src/vision/sampling.ts`에 따로 두고 **헌법 검사가
   막는다.**
2. **⚠️ 캡션을 되뱉기 판정에 넣으면 성공한 일기가 거부된다.** 005가 「신호가 들어간 줄은
   넣지 않는다」고 이미 적었고 **캡션은 신호 그 자체다** — 「창가에 놓인 커피잔」이 일기에
   나오는 것이 정확히 우리가 원하는 것이다. `instructionLines()`에 **한계 줄만** 넣는다.
3. **★ 5장 상한이 `n_ctx` 위험도 함께 막는다.** 캡션은 장당 한 줄이라 상한이 없으면
   컨텍스트를 넘긴다. 004의 200장은 프롬프트에서 한 줄로 요약되므로 같은 문제가 없었다.
4. **⚠️ `DownloadProgress`가 `character: Character`에 묶여 있다.** 003이 의도적으로 그렇게
   둔 자리이며, 사진 보는 모델은 캐릭터가 아니다. **`DownloadPort`는 이미 캐릭터를
   모르므로**(`start(key, url, …)`) 008의 타입을 건드리지 않고 우회했다 — **미룬 것이며
   잊은 것이 아니다.**

### 균일 선택 — 004와 의도적으로 다르다

`src/signals/collect.ts`가 `usable.slice(0, limit)`으로 **이른 시각부터** 자른다.
그대로 쓰면 **아침 사진 다섯 장만 보고 하루를 쓴다.**

**004는 「그날 사진이 몇 장인가」를 세고 011은 「하루가 어떠했는가」를 그린다.**
균등 분위로 고르며 **가장 이른 것과 가장 늦은 것이 반드시 들어간다.**

### 불확실성을 언어로 전달하지 않는다

캡션에 「틀릴 수 있다」를 붙이지 않는다. **휴대폰은 그 사진을 실제로 보았고**, 캡션은
004의 장수·좌표와 **같은 자격의 관측**이다. 붙이면 모델이 전부를 얼버무리게 되고,
005의 실측이 가르친 것은 **압력이 지어내기를 낳는다**는 것이었다.

**대신 값 쪽에서 막는다**: 몇 장을 보았는가(`considered`/`available`), 하루의 어느
때인가(캡션 줄의 시각), 읽지 못한 것과 없는 것의 구분.

### 헌법 검사에 규칙이 둘 늘었다

- `src/vision/`이 `diary/store`·`DiaryEntry`에 닿지 않는다(원칙 IV) — **「캡션이 일기
  품질을 올렸는지 재 보자」가 아주 자연스럽게 떠오르는 자리**이고, 옆 저장소가 이미
  쟀다(3.62 대 3.31)
- `src/vision/`이 `inference/sampling`을 import 하지 않는다(원칙 I)

**위반 주입 다섯을 전부 해 봤고 전부 걸렸다**: `VisionRunResult`에 `elapsedMs`(1개
실패), 캡션을 `instructionLines()`에(1개), `slice(0,5)`로 자르기(**9개**),
`vision` 실패를 `none`으로 낮추기(1개), `vision`이 일기에 닿기(헌법 검사).

### 옆 저장소에서 옮긴 것 / 옮기지 않은 것

`my-ollama`가 2026-08-10에 **같은 기기(SM-G986N)**에서 VLM 셋을 쟀다. 003이 캐릭터
로스터의 URL을 같은 저장소에서 옮겨 온 선례를 따른다.

| 옮겼다 | 옮기지 않았다 |
| --- | --- |
| 어느 모델을 쓰는가 (URL·파일명) | 캡션 품질 점수 (3.62 대 3.31) |
| 5장이 약 10초라는 사실 | 자기일치도·환각률 표 |
| mmproj 대소문자 함정 | 「캡션이 품질을 올리는가」의 결론 |

**주력은 LFM2.5-VL 450M, 후보는 SmolVLM 500M.** 후보는 **사람이 바꿔 넣는 기록이지
실행 중 대체가 아니다** — 003 FR-035가 대체 경로를 금지한다.

**⚠️ mmproj 파일명이 `450m` 소문자다.** 본체는 `450M`이고 **대문자 URL은 404다**
(2026-08-10 옆 저장소, 2026-08-22 재확인). 놓치면 「내려받기가 안 된다」로 나타나고
원인이 URL이라는 것을 알기 어렵다.

### 실측 / 짐작 (원칙 V)

| 값 | 지금 | 성질 |
| --- | --- | --- |
| 파일 크기 379,219,104 / 102,815,168 | **실측** (2026-08-22 `Content-Length`) | **실기기에서 받은 바이트 수와 일치했다** |
| ~~5장 ≈ 10초~~ | **★ 실측으로 뒤집혔다** — 캡션 5장에 **약 60초**다 | 옆 저장소의 10초를 여기서 쓸 수 없다 |
| 캡션 `temperature` 0.1 | **옆 저장소 실측** (세 VLM 모두 0.7을 이겼다) | 자기일치도 0.283 → 0.789. **여기서 재지 않았다** |
| 사진 상한 5장 | 위 10초에 근거했으나 **그 근거가 틀렸다** | 5장이 약 60초다. **상한을 다시 볼 근거가 생겼다** |
| `image_max_tokens` 256 / 1024 | **★ 효과가 실측됐다** — IMAGE 청크가 64 → 256토큰 | **다만 자세히 볼수록 더 지어냈다**(D6) |
| 캡션 `n_predict` 64 | **짐작** | 옆 저장소 캡션이 20~30토큰 |
| `n_ctx` 4096 (사진용) | **짐작이나 넘치지 않았다** | 사진 5장 + 캡션에서 `context_full`이 없었다 |
| md5 | **★ 채록했다** (위 표) | 두 번 받아 두 번 다 같았다 |

### ✅ 실기기에서 확인했다 (2026-08-22, SM-G986N, Android 13, USB adb, debug 빌드)

**기기 없는 검증 1190개가 통과하고, D1~D7을 실기기에서 확인했다.** release 검증(T064)과
그만두기(D8)가 남았다.

#### ★ D2가 결함을 잡았다 — 사진을 안 보고도 일기가 나왔다

**이 기능의 핵심 검증이 실제로 결함을 잡았다.** 처음 돌렸을 때 일기는 멀쩡히 나왔고
시간도 76초로 늘었으나(none은 39초), **사진을 하나도 보지 않은 상태였다.**

`on-device.ts`가 `resolvePath: async (photo) => photo.id`로 두고 **「기기에서 확인하며
정한다」고 주석에 적어 두었던 자리**다. 안드로이드에서 `Photo.id`는
`content://media/external/images/media/1000000871` 꼴의 contentUri이며(`expo-media-library`
57의 `Asset` 타입 주석이 그렇게 적는다), **네이티브가 그것을 파일로 열지 못한다.**

**조용히 실패한다** — 006의 `GenerationProbe`, 007의 끊긴 `stop` 배선, 009의 `day:` 한
줄과 **같은 종류**다. 드러나는 곳은 로그뿐이었다:

| 무엇 | 고치기 전 | 고친 뒤 |
| --- | --- | --- |
| 다섯 장 처리 시간 | **92밀리초** | 약 1.1초/장 |
| `has_media` | **0** | **1** |
| IMAGE 청크 | 없음 | **`n_tokens=64`** |
| 일기 프롬프트 | 511토큰 | **664토큰** |

**「일기가 나왔다」를 통과로 치지 말라는 quickstart D2의 경고가 정확히 이것을 잡았다.**

**고친 방법**은 004가 예고한 대로 계약을 넓힌 것이다 — 004의 `port.ts` 주석이
「시각 처리가 URI를 필요로 할 때 이 계약을 넓힌다 — 미리 열어 두지 않는다」라고 적어
두었고, 011이 그 자리를 열었다. `PhotoPort.filePathOf()`가 `Asset.getUri()`를 부르고
**`file://`를 떼어 낸다.**

- **`PhotoFacts`에 담지 않고 함수로 둔다.** 담으면 사진을 세기만 하는 004의 경로가
  경로를 들고 다니게 되고, 그때 「사진의 내용을 읽지 않는다」가 규율로만 남는다.
  **함수면 부르지 않는 쪽은 경로를 얻을 수 없다.**
- `__tests__/vision/photo-path.test.ts`가 **소스를 직접 읽어** 되돌아가는 것을 막는다 —
  `string`을 넘기는 것은 어느 쪽이든 옳으므로 **타입이 잡지 못한다.**

#### ★ D2 통과 — 두 일기가 확연히 다르다 (SC-001)

**같은 하루(2026-08-21, 사진 13장)를 같은 캐릭터(`quiet`)로 두 번 썼다.**

| | 「보지 않음」 (39초) | 「빠르게 봄」 (100초) |
| --- | --- | --- |
| 사진 내용 | **「무엇을 찍었는지는 알 수 없지만」** | **「검은색으로 보인다」**, 「sleeping bag」 |
| 시각 | 「오전 5시부터 자정 1시까지」 (범위) | **아침 5시 / 오전 10시 / 오후 4시 / 저녁 9시 / 밤 12시·1시** (장별) |

**★ 「검은색」이 정확한 관측이다.** 010의 템플릿 JPEG이 실제로 **거의 검은 이미지**이며
(`scripts/seed-template.jpg`, 54,590바이트 — 로그가 보고한 크기와 같다), 휴대폰이
**본 것을 그대로 적었다.** 지어낸 것이 아니다.

**⚠️ 그래서 심은 사진으로는 「캡션이 좋은가」를 알 수 없다**(010 FR-021). 검은 이미지를
검다고 말한 것은 「경로가 돈다」의 증거이지 품질의 증거가 아니다. **진짜 사진이 있는
하루가 필요하다.**

#### ★ D3 통과 — VLM을 닫고 캐릭터 모델을 여는 것이 된다 (미관측이던 자리)

**옆 저장소가 확인하지 못한 자리였다**(캡션이 원격, 일기가 폰이었다). 실측 결과
**된다** — `initMultimodal` → 캡션 5장 → `release` → 캐릭터 모델 적재 → 생성이
한 요청 안에서 끝까지 갔다. 크래시도 「생성 실패」도 없었다.

**⚠️ `narrative`(2.4B)로는 아직 안 해 봤다** — `quiet`(2.1B)으로만 확인했다.

#### ★ D5 통과 — 균일 선택이 하루를 그린다 (FR-007a)

`spread-day` 모양을 새로 만들어(사진 12장, 하루에 걸쳐 흩어짐) 확인했다. **기존 모양 중
가장 큰 `partial-location`이 정확히 5장이라 고르는 일 자체가 일어나지 않았다** —
`over-limit`(201장)은 010 실측에서 색인이 밀려 못 쓴다.

일기가 **아침 5시부터 밤 1시까지** 고루 말한다. **앞에서부터 잘랐다면 전부 아침이었을
것이다.**

#### ★ D6 답 — 두 깊이가 실제로 다르다. 다만 자세히 볼수록 더 지어낸다

`image_max_tokens`가 실제로 걸린다 — 로그의 IMAGE 청크가 **64 → 256토큰**으로 늘고
시간은 100초 → 119초가 됐다. **값이 짐작이었는데 효과가 실측으로 확인됐다.**

**⚠️ 그런데 「자세히 봄」의 일기가 원칙 II를 크게 어겼다.** 같은 검은 이미지를 보고:

> - 10시: 집에서 조용히 **책을 보는 장면**을 촬영했다. 주인의 손에 **책이 들려 있고**
> - 21시: 식사 후 주인은 **식탁 위에 놓인 달력**을 유심히 보는 모습을 포착했다.
>   **달력에는 다음 주 일정이 적혀 있다.**

**검은 이미지에 책도 식탁도 달력도 없다.** 「빠르게 봄」은 「검은색이다」로 정직했는데
**자세히 볼수록 지어냈다** — 사진 토큰이 늘면 모델이 그 자리를 채우려 한다.

**⚠️ 어느 쪽이 나은지 판정하지 않는다**(원칙 IV). **다른가**를 물었고 다르다.
다만 **이 관측은 심은 검은 이미지에서 얻은 것**이므로, 진짜 사진에서도 같은지는
아직 모른다.

#### ★ D7 답 — 캡션이 영어이고 그것이 일기에 새어 나온다 (미결이던 자리)

캡션 프롬프트가 영어이고(`Describe what is visible in this photo…`) 모델도 영어로
답한다. **한국어 일기에 「sleeping bag」이 그대로 들어왔다** — 옆 저장소가 gemma3·qwen3
에서 관측한 것과 같은 양상이다.

**아직 고치지 않았다.** 캡션을 한국어로 뽑을지는 **진짜 사진에서 한 번 더 보고 정한다** —
검은 이미지의 「sleeping bag」은 모델이 무엇을 본 것인지조차 불분명하다.

#### ★ D4 통과 — 볼 것이 없으면 사진 보는 모델을 열지도 않는다

| 상황 | 캡션 시도 | mtmd 열기 | 시간 | 일기가 말한 것 |
| --- | --- | --- | --- | --- |
| 사진 0장(`empty`) + 「빠르게 봄」 | **0회** | **0회** | 39초 | 「사진 앱을 열었지만 **아무 사진도 보이지 않고**」 |
| 권한 없음 + 「빠르게 봄」 | **0회** | **0회** | 31초 | 「사진 앱이 켜져 있지만 **아직 …볼지 모르는 상황**」 |
| 사진 13장 + 「빠르게 봄」 | 5회 | 1회 | 100초 | 장별 서술 |

**★ 원칙 V가 글까지 정확히 도착했다.** 권한이 없는 하루를 **「사진이 없었다」로 적지
않았다** — 「모르는 상황」이라고 썼다. 004가 값에서 지킨 `none`/`unknown` 구분이 화면을
지나 일기의 문장까지 살아 있다.

#### ★ D8 통과 — 캡션 도중에 끊어도 저장된 일기가 그대로다

**007·009보다 잡기 쉬웠다** — 캡션에 약 60초가 걸리므로 시작 12초 뒤(다섯 장 중 **네
장째**를 읽던 중)에 눌렀다. 007은 생성이 2.4초라 900밀리초 안에 눌러야 했다.

| 무엇 | 결과 |
| --- | --- |
| 화면 | 목록으로 돌아갔다 |
| 저장된 일기 | **md5가 바이트 단위로 같다**(`f1e2a2c7…`) — 덮어쓰이지 않았다 |
| 그만둔 하루 | **새 파일이 생기지 않았다** — 일기가 정확히 셋 그대로 |

#### ★ 목록에서 사진 세 갈래를 한 화면에서 봤다 (원칙 V)

**004가 값에서 가른 셋이 화면에 나란히 떴다.** 007이 `none`을 미확인으로 남기고
009가 우연히 하나를 본 뒤, **셋이 동시에 보인 것은 처음이다.**

| 하루 | 목록 | 뜻 |
| --- | --- | --- |
| 2026-08-21 | **「사진 13장」** | `known` — 실제로 세었다 |
| 2026-08-20 | **「사진 모름」** | `unknown` — 권한 없이 쓴 하루 |
| 2026-08-19 | **「사진 없음」** | `none` — 물어봤고 0장이었다 |

상세 화면도 같다 — 권한 없이 쓴 하루가 **「사진: 모른다」**이지 「없었다」가 아니다.

#### ⚠️ 실측된 시간 (합성 하루 기준, 010 FR-020)

| 무엇 | 시간 |
| --- | --- |
| 사진 보는 모델 내려받기 (480MB, 두 파일) | **약 45초** |
| 캡션 5장 + 일기 (`quiet`, quick) | **100초** |
| 캡션 5장 + 일기 (`quiet`, detailed) | **119초** |
| 일기만 (사진 안 봄) | **39초** |
| 사진 0장 / 권한 없음 | 39초 / 31초 |

**옆 저장소의 「5장 약 10초」와 어긋난다** — 캡션에만 약 60초가 든 셈이다. 옆 저장소는
같은 기기였으나 그때 잰 것을 여기서 그대로 쓸 수 없다는 것이 확인됐다.

#### ★ md5를 채록했다 (FR-031)

| 자산 | md5 | 바이트 |
| --- | --- | --- |
| `v1` (본체) | `b0f40eda778e7563d8bc8a64be19134d` | 379,219,104 |
| `v2` (mmproj) | `7e8624e77234ee00c3c2f918220070c9` | 102,815,168 |

**두 번 받아 두 번 다 같은 값이었고** 바이트 수도 `Content-Length`와 정확히 일치했다.
**지문이 채록되면서 `prepareVision()`이 실제로 검증하게 된다** — 그전까지는 무엇이 오든
통과시켰고 그 사실이 빈 문자열로 드러나 있었다.

#### ✅ 화면이 원칙 III·IV를 지킨다

- **준비 자리가 한 줄이다** — 「사진을 보는 데 필요한 것 / 쓸 수 있음」. **파일이 둘인
  것이 드러나지 않는다**(FR-026, SC-008). 모델명·파일명·크기도 없다
- **생성 중 화면에 「쓰고 있다」와 「그만두기」뿐이다** — 백분율·초·「사진 3/5」·캡션
  본문이 **하나도 없다**(FR-011·034, SC-012)

#### ⚠️ 실기기에서 겪은 함정

- **`adb kill-server`나 기기 목록 변화가 `adb reverse`를 지운다.** 지워지면 debug 앱이
  **RN 레드박스**(`loadJSBundleFromAssets`)로 죽는다 — 앱 결함으로 오독하기 쉽다.
  `adb reverse --list`로 확인한다.
- **기기가 둘 붙으면 `adb shell`이 모호해진다.** 둘째가 `unauthorized`여도 그렇다.
  **`-s <시리얼>`을 붙인다.**
- **`adb`는 Windows 실행 파일이라 Git Bash의 `/tmp`를 모른다.** `adb pull`의 목적지는
  `C:/…` 꼴로 준다.
- **`uiautomator dump`의 한글이 콘솔에서 뭉개진다** — CP949다. **UTF-8로 직접 써야**
  읽힌다(Maestro의 `-Dfile.encoding=UTF-8`과 같은 성질).

### ✅ release 빌드에서도 확인했다 (2026-08-22, SM-G986N, versionCode=7)

**빌드 18분 41초**(359개 태스크 중 331개 실행 — `prebuild --clean`이 캐시를 지워
전체 빌드였다). APK **173MB**. 서명이 **`CN=alpharium`**이고 키는 커밋되지 않았다.

#### ★★ `initMultimodal`의 JNI 심볼이 R8·ProGuard에서 살아남는다

**이 기능에서 가장 큰 미지수였다.** 005가 `initLlama`에서 통과했으나 **멀티모달
심볼은 처음**이었고, 깨지면 release에서만 죽어 debug 검증이 전부 무의미해질 자리였다.

```
Multimodal context initialized successfully with mmproj: …/files/models/v2.bin
```

`UnsatisfiedLinkError`도 `NoSuchMethodError`도 크래시도 없다.

| 무엇 | release에서 |
| --- | --- |
| Metro 끄고 `adb reverse` 지운 채 뜨는가 | **뜬다** — `Unable to load script`가 없다 |
| 진단 화면 | **없다** — prod로 판정된다(001 SC-013) |
| 두 모델 내려받기 + md5 검증 | **된다** — 채록한 지문이 실제로 검증된다 |
| `initMultimodal` | **★ 산다** |
| 캡션 5장 | **된다** — IMAGE 청크 64토큰, `has_media=1` |
| VLM→캐릭터 전환 | **된다** — 92초에 일기까지 |
| `testID` | **산다** — 008·009에 이어 세 번째 |
| 사진 설정 셋 | 보이고 골라진다. **새로 깔면 「보지 않음」이다**(FR-018) |
| 준비 자리 | 「사진을 보는 데 필요한 것」 **한 줄**(FR-026) |

**release가 낸 일기도 사진을 실제로 보았다** — 「검은 색조가 주를 이루는 물체」,
「검은색이 주된 색상임을 확인했다」로 **심은 검은 이미지를 그대로 적었다.**

**⚠️ 다만 「책상에 앉아있는 모습」·「책상 위에 노트북」은 지어낸 것이다** — 검은
이미지에 없다. debug의 「자세히 봄」에서 본 것과 같은 양상이 「빠르게 봄」에서도
나왔다. **고칠 자리는 프롬프트이며 판정 갈래를 늘리지 않는다**(원칙 IV).

#### ⚠️ debug ↔ release 전환에서 일기와 모델이 사라진다

서명이 달라 덮어 설치가 거부되고(`INSTALL_FAILED_UPDATE_INCOMPATIBLE`), 지우고 깔면
**일기 3개와 받아 둔 모델 약 2GB가 함께 사라진다.** 006·007이 관측한 것과 같으며
방향만 바뀐 것이다. **010이 심은 사진은 앱 밖이라 살아남았다.**

### 아직 확인하지 않은 것 (원칙 V)

- **`narrative`(2.4B)로 D3** — 가장 큰 캐릭터에서도 전환이 되는가
- **진짜 사진에서의 캡션** — 지금까지 본 것은 **전부 010이 심은 검은 이미지**다.
  **캡션 품질도, 캡션 언어(D7)도, 깊이 차이(D6)도 그 위에서 얻은 관측이며**,
  **품질을 결론짓지 않는다**(010 FR-021)
- **사진 보는 모델과 캐릭터를 동시에 받을 수 있다** — 003의 「한 번에 하나」가 이 쌍에는
  적용되지 않는다. **미룬 것이며 잊은 것이 아니다**

## 012 — 오늘의 일기 (2026-08-22, **✅ 실기기·release 확인 완료**)

**006이 박아 둔 「정오 이후에도 오늘을 못 쓴다」를 사용자가 정오만 지나면 오늘을 골라
쓸 수 있게 했다.** `pipeline.ts`의 게이트가 `isDayClosed` 대신 `isDayWritable`을
보게 바꾼 것이 이 기능의 전부이며, 나머지(축 제외·덮어쓰기 확인·상한 제거)는 그
게이트가 열리면서 함께 드러난 표면이다.

기기 없는 검증 **1254개**(011의 1190에서 +64)가 통과하고, **실기기·release
확인을 마쳤다**(SM-G986N, Android 13, USB adb, versionCode=8).

### 무엇이 바뀌었나

- **정오 게이트**: `day-boundary.ts`에 `WRITABLE_FROM_HOUR`(12, 비export)와
  `isDayWritable(day, now)`가 생겼다. `pipeline.ts` 1단계가 `isDayClosed` 대신
  이것을 본다 — 정오 이후 오늘이 더 이상 `day-not-closed`로 막히지 않는다.
  `selectableDays()`도 정오를 넘기면 그그제 대신 오늘을 넣는다.
- **"하루의 끝" 문장**(헌법 원칙 II MUST): `DAY_STILL_OPEN`("오늘은 아직 끝나지
  않았다. 이 기록 뒤에 무슨 일이 더 있었는지는 알 수 없다.")이 사진 권한과 무관하게
  프롬프트에 실린다(FR-004). `DayPicker`가 정오 이전엔 "정오(12시)부터 쓸 수 있다"를
  포함한 안내를 그린다(`/speckit-analyze` C1이 잡은 갭 해소).
- **축 제외**: `USER_VISIBLE_SIGNAL_AXES`(`{photos: true, places: true, steps: false,
  battery: false, connectivity: false}`, 사람이 쓴 상수)로 걸음·배터리·연결이
  프롬프트·상세 화면에서 사라졌다. 진단 화면(`SignalProbe.tsx`)은 그대로 다섯 축을
  보인다 — 헌법 검사(`constitution-rules.ts`)가 이 파일이 그 상수를 참조하지
  못하게 막아 이 경계를 이중으로 지킨다.
- **사진 상한 제거**(FR-014·015): `DEFAULT_PHOTO_LIMIT`과 잘림 판정을 걷어냈다.
  조회에 성공하면 그 구간의 사진 전부가 담기고 `complete`는 언제나 `true`다.
  조회 실패는 여전히 `unknown`.
- **덮어쓰기 확인**(FR-011): `AppScreen`에 `confirm-overwrite` 갈래,
  `OverwriteConfirmScreen` 신설. `toWriting()`은 여전히 인자를 받지 않는다 —
  「이미 있다」를 아는 것은 화면이고 쓰기를 시작하는 함수는 모른다.

### ★ 이 기능에서 가장 위험했던 결함 — 화면은 완벽해도 게이트가 그대로면 조용히 막힌다

quickstart.md A1이 미리 경고한 대로였다: 화면이 오늘을 완벽하게 그려도 파이프라인
게이트가 옛 `isDayClosed`로 남아 있으면 「일기 쓰기」를 누르는 순간 조용히
`day-not-closed`로 막힌다. **위반 주입에서 실제로 이것부터 확인했다** — day-boundary
계약 §3의 행이 정확히 걸렸다.

### ⚠️ `DAY_STILL_OPEN` 문장이 신호 빈약한 하루에서 되뱉기를 유발할 수 있다 (실기기 관측)

**T041 실기기 검증 중 세 번 시도 중 두 번이 `echo`로 거부됐다**(2026-08-22, `quiet`,
신호 전부 `unknown`인 오늘). `DAY_STILL_OPEN`이 `instructionLines()`에 포함되고
되뱉기 판정(`MIN_ECHO_LENGTH` 12자 이상 그대로 포함)의 비교 대상이 되는데, 신호가
거의 없는 하루에서는 모델이 쓸 재료가 이 문장 하나뿐이라 그대로 되뱉을 유혹이 큰
것으로 보인다.

**거부된 시도에서 파일이 건드려지지 않는 것도 함께 확인했다**(md5 동일) — 원칙 I의
방어가 정상 작동한다. 세 번째 시도에서 성공했고, 그 일기는 짐작을 짐작으로
적었다("아마도 바쁘게 움직였을지도 모르겠다", "미소가 번졌을지도 모른다") — SC-002a는
지키되 `echo` 재시도가 필요했다는 점이 새로 관측됐다. **고칠 자리는 프롬프트이며
012의 범위가 아니다**(005의 자리, 원칙 IV) — 관찰만 남긴다.

### ✅ 실기기에서 확인했다 (2026-08-22, SM-G986N, Android 13, USB adb, debug 빌드)

- **★ D2 — 정오 이후 오늘을 실제로 골라 생성했다.** 21:11 KST(정오 한참 이후)에
  "언제를 쓸까"에 **오늘(2026-08-22)이 이미 「고름」으로 선택돼 있었다** — D1도
  함께 확인됨(오늘·어제·그제 셋, 그그제는 없음). 「일기 쓰기」를 눌러
  `day-not-closed`로 막히지 않고 실제로 생성이 돌았다.
- **★ D3·D4 — 날짜가 오늘이고 "아직 안 끝났다"가 본문에 있다.** 저장된 일기가
  `2026-08-22` 날짜로 목록·상세에 나타났고, "오늘은 아직 끝나지 않았다는 주인의
  말만이 남아 있다"로 마무리됐다.
- **FR-005 관찰**: 그 문장 뒤로 아직 일어나지 않은 일을 단언하지 않았다 — "잠시
  바깥을 걷다가 돌아왔음을 느낄 수 있을 뿐이다"처럼 짐작을 짐작으로 적었다.
- **D5 — 사진 권한 없이도 성립한다.** 이번 검증 전체가 사진 권한 미부여
  상태(`아직 묻지 않음`)에서 진행됐고, 그 상태로 만든 일기에도 "아직 끝나지
  않았다"가 그대로 있었다.
- **D6·D7 — 걸음·배터리·연결이 사용자 화면에서 사라지고 진단엔 남는다.** "이 일기가
  본 것"엔 사진·다닌 자리뿐이었다. `SignalProbe.tsx` 소스에는 `signal-steps`·
  `signal-battery`·`signal-connectivity` testID가 그대로 있다.
- **D9~D11 — 덮어쓰기 확인이 실제로 한 번 더 막는다.** 이미 일기가 있는 하루에
  「일기 쓰기」를 누르니 곧바로 생성되지 않고 "이 날의 일기가 이미 있다. 덮어쓸지
  확인이 필요하다" + 취소/확인만 있는 화면이 떴다(D13, 진행률·모델 정보 없음).
  **취소**하면 목록으로 돌아가고 파일이 그대로였다(md5 `025f3cf0...` 유지,
  `run-as`로 직접 확인). **확인**하면 "쓰고 있다"가 뜨고 실제로 생성이 돌아
  파일이 덮어써졌다(md5 `1996f63d...`로 변경, 파일 하나만 존재).
- **★ D12 — 사진 상한이 없다.** 010의 `spread-day`(12장)를 심어 기존 진짜 사진과
  합쳐 총 18장이 된 하루에서 "사진: 18장, 다닌 자리: 10곳"이 표시되고 **"전부 못
  봤다"류 경고가 전혀 없었다**. `over-limit`(201장)은 010의 색인 실패 전례(322초,
  150장만 색인)가 있어 시도하지 않았다 — 18장으로도 상한 없음은 이미 관측됐다.

### ⚠️ debug 첫 빌드가 도는 동안 겪은 함정 둘 (2026-08-22 실측)

1. **release 앱 위에 debug를 덮어 설치하면 거부된다** — `INSTALL_FAILED_UPDATE_
   INCOMPATIBLE`. 006·007·008·009·011이 이미 관측한 것과 같은 성질이며, 여기선
   기기에 011의 release(`CN=alpharium`)가 남아 있던 상태에서 재현됐다. 지우고
   다시 깔아야 했다.
2. **Metro 캐시가 스테일이면 "Loading from localhost:8081..."에 영구히 머문다**
   (007·009가 이미 기록한 것과 같은 현상). `npx expo start --dev-client --clear`로
   캐시를 지우자 정상적으로 "Bundling ..."으로 넘어갔다. `EXPO_PUBLIC_APP_ENV=dev`를
   셸에서 직접 준 값은 `.env.development`(로드되는 파일, 안에는 `local`)에
   **덮어써지지 않는다** — `@expo/env`의 `load()`가 이미 설정된 `process.env` 값은
   보존한다(소스 확인, `node_modules/@expo/env/build/index.js:400`). 이전 세션
   기록의 우려는 이 버전에서 실측으로 반증됐다.

### ✅ release 빌드에서도 확인했다 (2026-08-22, SM-G986N, versionCode=8)

**빌드 17분 50초**(359개 태스크 중 331개 실행 — `prebuild --clean`이 캐시를 지워
011과 비슷한 전체 빌드였다). 서명이 **`CN=alpharium`**이고(`apksigner verify
--print-certs` 확인) 키는 커밋되지 않았다.

- **★ Metro 끄고 `adb reverse` 지운 채로 뜬다.** `Unable to load script`가 없고
  진단 화면도 없다(prod로 판정됨, 001 SC-013).
- **★ 캐릭터 내려받기·md5 검증이 release에서 돈다** — `quiet`이 "쓸 수 있음"이 됐다.
- **★ D2 재확인 — release에서도 정오 이후 오늘 생성이 된다.** 오늘이 이미
  「고름」으로 선택돼 있었고, 생성한 일기가 "오늘은 여전히 진행 중이다"·"오늘은
  여전히 미완의 하루다"로 D4를 만족했다. 지어낸 것도 없었다("본 것은 없다",
  "알 수 없지만"으로 정직하게 적음).
- **★ D9 재확인 — release에서도 덮어쓰기 확인이 뜬다.** "이 날의 일기가 이미 있다.
  덮어쓸지 확인이 필요하다" + 취소/확인만 있는 화면이 debug와 동일하게 나왔다.
- **⚠️ release는 `run-as`가 안 된다**(`package not debuggable`) — 006 이후 매
  release마다 반복되는 제약이며, D9의 파일 검증(md5)은 화면 관찰로 갈음했다.

### ⚠️ 아직 확인하지 않은 것 (원칙 V)

- **D8 — 정오 이전 안내.** 검증 시각이 이미 정오를 한참 지나 있었고, 기기 시각을
  바꾸려면 root가 필요하며 `adb shell date`가 조용히 실패한다(007·009가 이미
  기록한 제약). **기기 없는 테스트(day-boundary 계약)가 이 갈래의 주된 검증이다** —
  실기기에서는 "지금이 정오 이후인 상태" 하나만 볼 수 있었다.
- **정오 경계 자체를 넘나드는 순간의 화면 갱신**(Edge Case "11:59→12:00") — 같은
  이유로 확인하지 못했다.
- **`narrative`·`imaginative` 등 다른 캐릭터에서의 D2** — `quiet`으로만 확인했다.

## 코드를 어디에 두는가

```
src/
├── config/       환경 판정, 추론 위치 규칙, 하루 경계
├── inference/    추론 어댑터 (온디바이스 / 데스크톱 서버)
├── signals/      하루치 신호. 사진은 실제로 수집한다 (나머지는 unknown)
├── vision/       사진의 내용을 읽는다 (011). **캐릭터와 무관한 모델 하나**
├── diary/        일기의 모양, 파이프라인, 저장
├── models/       캐릭터→모델 파일 매핑, 내려받기·검증·삭제
├── diagnostics/  진단 정보 수집과 출력 경로
└── ui/           화면

scripts/          헌법 검사, 실기기 테스트 실행기
__tests__/        기기 불필요 테스트 (항상 돈다)
.maestro/         실기기 테스트 (기기 있을 때만)
```

`src/signals/`와 `src/diary/`는 002에서 열렸다. **004가 `src/signals/`의 안을 채웠고,
005가 `src/diary/`와 `src/inference/`의 생성 경로를 채웠다.**

- `src/signals/` — **사진은 실제로 수집한다**(004). `photos`는 미디어 라이브러리에서 오고,
  `places`는 사진에 딸린 좌표로 채워진다. **`steps`·`battery`·`connectivity`는 `unknown`이며
  그것이 004의 결론이지 미완성이 아니다** — 되짚을 통로가 없어 기록을 쌓는 계층이 먼저
  필요하다. `fake.ts`는 테스트·개발용이며 **제품 경로가 아니다**. 이것이 헌법 원칙 I의
  "미리 만들어 둔 응답"으로 자라지 않도록 `src/ui/`에서 import 하지 않는다
- `src/diary/` — 요청·일기·파이프라인·저장에 **프롬프트와 판정**이 더해졌다(005).
  파이프라인이 **`generation`을 지나 저장까지 간다**
  - `prompt.ts` — **헌법 원칙 II의 유일한 통과 지점.** 화자 규칙이 여기 하나뿐이고,
    `unknown`/`none`을 서로 다른 문장으로 옮긴다. **캐릭터에서 오는 것은 언어뿐이다** —
    성격 지시를 넣으면 관측된 성격이 아니라 우리가 지어낸 성격이 된다(원칙 III)
  - `acceptance.ts` — **원칙 I의 마지막 방어선이자 원칙 IV가 가장 쉽게 무너지는 자리.**
    거부 갈래가 **넷뿐이고**(`empty`/`echo`/`language`/`unfinished`) 그 수가 테스트로
    못 박혀 있다. **임계값·유사도·점수를 쓰지 않는다** — 다섯 번째 갈래를 넣으려면
    `contracts/acceptance.md`를 먼저 읽게 되어 있다

`src/models/`는 003에서 열렸다. **이 자리가 처음으로 캐릭터와 모델 파일을 잇는다** —
그래서 여기가 헌법 원칙 III의 최전선이다.

- `src/models/roster.ts` — **캐릭터→모델 매핑의 유일한 자리.** 001의 `policy.ts`가 원칙 I의
  방어선을 한 곳에 모은 것과 같은 구조다. `allAssets()`도 `characterFor()`도 두지 않는다 —
  전자가 있으면 "다섯을 다 받자"가 한 줄로 가능해지고 그것이 헌법 로스터 위반이다
- `src/models/readiness.ts` — 준비 상태를 넷으로 가르는 **순수 함수**. 파일이 부분적으로
  존재하는 상태 때문에 002의 `SignalValue`보다 하나가 많다
- `src/models/expo-port.ts` — 기기에 닿는 유일한 자리. 나머지는 대역으로 기기 없이 검증된다

**로스터의 크기는 다섯 개 다 찼고 `quiet`의 지문(md5)은 실기기에서 채록됐다**(2026-08-14,
SM-G986N). 나머지 넷의 md5는 여전히 비어 있으며 **그것이 옳은 상태다** — 미리 적는 지문은
어디서 왔든 짐작이고, 첫 내려받기에서 받은 파일에서 채록하기로 했다. 통과시키려고 그럴듯한
숫자를 넣으면 헌법 원칙 V가 깨진다.

`src/inference/`는 001에서 열렸고 **005가 실제 추론을 채웠다.**

- `src/inference/llama-port.ts` — **기기에 닿는 유일한 자리이자 원칙 IV의 경계다.**
  001~004에서 원칙 IV는 「우리가 측정 코드를 안 쓴다」로 지켜졌지만, 여기서는 **네이티브가
  요청하지 않은 지표를 결과에 담아 보낸다**(`timings`·`tokens_predicted`·`per_second`).
  그래서 방어가 **「경계에서 버린다」**여야 한다 — `RunResult`가 `{ text, ending }` 둘뿐인
  것이 그 방어이며, **자리가 없으면 담을 수 없다**
- `src/inference/sampling.ts` — 온디바이스와 데스크톱이 **공유하는 유일한 자리**(원칙 I의
  「동일한 샘플링 파라미터」). 각자 만들면 조용히 어긋난다
- `src/inference/engine-port.ts` — 적재·실행·정리의 계약. `Ending` 다섯 갈래

`src/vision/`은 011에서 열렸다. **캐릭터 로스터와 별개의 자리다.**

- `src/vision/roster.ts` — 사진 보는 모델의 출처. **`models/roster.ts`와 서로 import
  하지 않는다** — 합치면 「캐릭터가 사진을 본다」는 잘못된 모양이 코드에 생긴다
- `src/vision/select.ts` — 5장을 **하루에 걸쳐 균일하게** 고른다. **004의
  `slice(0, limit)`과 의도적으로 다르다** — 앞에서부터 자르면 아침만 본 채 하루를 쓴다
- `src/vision/vision-port.ts` — 기기에 닿는 유일한 자리이자 **원칙 IV의 두 번째 경계**.
  `VisionRunResult`의 자리가 `text` 하나뿐인 것이 방어다
- `src/vision/sampling.ts` — **`inference/sampling.ts`를 재사용하지 않는다.** 캡션이
  그것을 같이 쓰면 캡션을 위해 온도를 낮출 때 **일기 생성이 함께 바뀐다**(원칙 I)

**아직 없고 앞으로 생길 자리** — 빈 폴더를 미리 만들지 않는다.

- **일기를 읽는 화면** — 목록·상세. 005는 진단 경로에만 생성 자리를 두었다
- **데스크톱 서버 전송 경로** — 005가 「무엇을 보낼지」(`serverRequestFor`)는 확정했고
  「어떻게 보낼지」는 미뤘다. **거기에 fetch를 붙이는 사람은 반드시 그 함수를 거쳐야
  한다** — 거치지 않으면 원칙 I의 「동일한 프롬프트」가 조용히 깨진다
- **사진 이외의 신호 수집** — 배터리·연결은 기록을 쌓는 계층이 먼저 필요하다. 걸음 수는
  안드로이드가 기간 조회를 주지 않아 되짚을 방법이 없다
- 프롬프트와 화자 규칙 (헌법 원칙 II) — 실제 생성이 붙을 때 `src/diary/`에 생긴다
- 실제 추론 실행 — `generate()`는 003이 끝나도 여전히 `not-implemented`다. 모델 파일이
  기기에 있는 것과 그것을 돌리는 것은 다른 축이다

**측정·채점 코드를 둘 자리는 없다.** 모델 출력을 점수로 매기거나 여러 모델을 비교하는
코드는 위 어느 자리에도 속하지 않는다(헌법 원칙 IV). 필요하면 별도 저장소에서 한다.
`scripts/check-constitution.mts`는 설정 위반을 잡는 것이지 모델 출력을 재지 않는다 —
그 경계가 `scripts/constitution-rules.ts` 주석에 적혀 있다.

### 지켜야 할 경계

**`process.env`는 `src/config/environment.ts`에서만 읽는다.** 다른 곳에서 환경을 다시
판정하면 단일 판정이 무너진다(FR-009a). 환경 값이 필요하면 그 모듈의 함수를 쓴다.

**추론 위치는 `src/inference/select.ts`에서만 고른다.** 이 함수를 거치지 않고 어댑터를
직접 만들어 쓰지 않는다(FR-025). 그래야 일기 생성 코드가 자신이 어디서 도는지 모른 채로
남고, dev·prod의 온디바이스 강제를 한 지점에서 검증할 수 있다.

**`src/config/policy.ts`가 헌법 원칙 I의 방어선이다.** dev·prod에서 데스크톱 서버가
허용되지 않는다는 규칙이 이 파일 한 곳에만 있다. 규칙을 다른 곳에 복제하지 않는다.

**하루는 04:00에 닫히고, 그 값은 `src/config/day-boundary.ts`에만 있다**(FR-021a).
자정이 아니다 — 00:30은 전날이고 04:00은 당일이다. 새벽 활동이 전날 일기에 붙는다.
하루가 필요하면 `dayOf(instant)`와 `isDayClosed(day, now)`를 쓴다. 경계를 다른 곳에서
다시 계산하면 신호 수집과 일기 생성이 서로 다른 하루를 보게 되고, 그 순간 일기의 근거가
어긋난다. **두 함수 모두 "지금"을 인자로 받는다** — 안에서 `new Date()`를 부르면 경계값을
테스트할 수 없다.

**모르는 것을 기본값으로 채우지 않는다**(FR-003, 헌법 원칙 V). `SignalValue<T>`는
`known`/`none`/`unknown` 셋을 가르며, `valueOr(signal, 0)` 같은 편의 함수를 만들지 않는다.
만드는 순간 걸음 수가 0이 되어 "걷지 않았다"는 거짓이 된다 — 안드로이드는 기간 걸음 수를
주지 않으므로 `unknown`이 정상 상태다.

**실패가 텍스트를 반환하지 않는다**(FR-016, 헌법 원칙 I). `GenerationFailure`의 어느
갈래에도 `text` 필드가 없다. "일기를 생성할 수 없습니다" 같은 플레이스홀더도 금지다 —
그럴듯한 텍스트가 일기 자리에 들어가는 순간 가짜 일기와 구분이 사라진다. **005가 갈래를
넷 더했고 전부 이 불변식을 지킨다.**

**신호가 없는 하루의 일기는 서로 비슷해도 된다**(006 FR-037a). 볼 것이 없는 하루에
휴대폰이 정직하게 쓸 수 있는 말은 사실상 하나뿐이며 그것이 그날의 진실이다. **매번 다른
글이 나온다면 그 차이는 모델이 지어낸 것이다** — 입력이 같은데 출력이 다르면 다른 부분의
출처는 기록이 아니다. 「비슷하다」와 「저장된 것을 다시 보여준다」는 다르다. 후자는 원칙
I이 금지한 캐싱이고, 전자는 매번 실제로 생성하되 결과가 닮은 것이다. **일률적인 것을
결함으로 읽고 다양성을 넣으려 하지 않는다** — 그 순간 지어내기가 시작된다.

**프롬프트는 `src/diary/prompt.ts`에만 있다**(005 FR-013b). 데스크톱 어댑터도 이것을
부른다. **캐릭터에서 오는 것은 출력 언어뿐이며**, 성격 지시를 넣지 않는다 — 성격은 모델의
성질이지 프롬프트가 만드는 것이 아니다(원칙 III).

**출력 판정의 갈래는 넷이고 늘리지 않는다**(005 FR-018b). `empty`/`echo`/`language`/
`unfinished`뿐이며 **테스트가 그 수를 직접 센다.** 「너무 짧다」·「반복이 많다」는 품질
판단이고, 임계값을 두는 순간 그것이 점수이며 채점 코드의 시작이다(원칙 IV — 되돌리기의
이유). 늘리려면 `specs/005-diary-generation/contracts/acceptance.md`를 먼저 고친다.

**네이티브 추론 결과의 지표를 경계 밖으로 내보내지 않는다**(005 FR-011). `llama.rn`의
`completion()`은 `timings`·`tokens_predicted`를 **요청하지 않아도 준다.** `llama-port.ts`가
`{ text, ending }`만 꺼내고 나머지를 버린다 — 이 파일이 유일한 경계다.

**생성 중인 글을 화면에 보여주지 않는다**(005 FR-028b). 토큰 콜백을 `completion()`에
**아예 넘기지 않으므로** 스트리밍 경로가 코드에 존재하지 않는다. 넘기는 순간 판정을
통과하지 않은 글이 화면에 오를 수 있고, 그것이 원칙 I의 방어선을 화면 쪽에서 우회하는
경로다.

## 환경은 셋이다

| 환경 | 어디서 | 추론 |
| --- | --- | --- |
| `local` | 개발자 기계 시뮬레이터 | 데스크톱 서버 (기본값), 실기기 연결 시 온디바이스도 가능 |
| `dev` | 실기기, 개발 빌드 | 온디바이스만 |
| `prod` | 실기기, 배포 빌드 | 온디바이스만 |

환경은 실행 시점에 `EXPO_PUBLIC_APP_ENV`로 정해진다. 빌드는 하나다.

**Expo Go로는 실행할 수 없다.** 네이티브 추론 모듈(`llama.rn`)이 Expo Go에 없기
때문이다. 헌법 원칙 I이 온디바이스 추론을 타협 불가로 못 박았고, 그 귀결로 development
build가 필요하다. `npx expo run:android`를 쓴다.

## release 빌드와 서명

**손으로 설치할 수 있는 배포물을 만드는 절차다**(006). 스토어 등록은 범위 밖이다.

### 서명 키 (최초 1회)

**⚠️ 이 키를 잃으면 이미 설치된 앱을 덮어쓸 수 없다.** 지우고 다시 깔아야 하고 그때
사용자의 일기가 함께 사라진다. **저장소 밖에 백업한다.**

```
keytool -genkeypair -v -keystore <경로>/alpharium.jks   -alias alpharium -keyalg RSA -keysize 2048 -validity 10000
```

**⚠️ 원본은 저장소 밖에 두고, `android/app/alpharium.jks`에는 사본을 놓는다**
(2026-08-20 실측). `android/`는 `prebuild --clean`이 **통째로 지우므로 거기 둔 키가
함께 사라진다** — `.gitignore`가 막아 주므로 커밋되지 않는다는 것과, 그 자리가
안전하다는 것은 다른 말이다. 지금 원본은 `~/.alpharium-signing/`에 있다.

```
mkdir -p ~/.alpharium-signing
cp android/app/alpharium.jks ~/.alpharium-signing/     # 원본 보관
cp ~/.alpharium-signing/alpharium.jks android/app/     # prebuild 뒤 되돌리기
```

비밀번호는 `~/.gradle/gradle.properties`에 적는다 — **저장소가 아니다**:

```
ALPHARIUM_STORE_PASSWORD=<비밀번호>
ALPHARIUM_KEY_PASSWORD=<비밀번호>
```

**서명 설정은 `plugins/with-release-signing.js`가 선언으로 넣는다.**
`android/app/build.gradle`을 직접 고치지 않는다 — `android/`는 gitignore된 생성물이라
저장소에 남지 않고 `prebuild --clean`에 지워진다.

### 빌드

```
npx expo prebuild --platform android --clean
cp ~/.alpharium-signing/alpharium.jks android/app/     # ★ prebuild가 지웠다
cd android && NODE_ENV=production ./gradlew assembleRelease
```

**`--clean`을 건너뛰지 않는다** — 004에서 이것 때문에 권한이 빠진 APK가 설치됐다.
**가운데 줄을 건너뛰지 않는다** — `prebuild --clean`이 키를 지웠기 때문이다.
**키가 없을 때 gradle이 어떻게 되는지는 아직 관측하지 않았다**(원칙 V) — 실패한다고
짐작하지 말고, 서명은 언제나 `apksigner`로 확인한다(아래 표).
**`NODE_ENV=production`이 필요하다** — Expo는 `NODE_ENV`로 env 파일을 고르므로
그것이 없으면 `.env.production`이 로드되지 않고 `EXPO_PUBLIC_APP_ENV`가 비어
앱이 「이 빌드는 잘못 만들어졌다」로 뜬다.

산출물: `android/app/build/outputs/apk/release/app-release.apk`

### 확인 — 빌드 성공을 믿지 않는다

| 무엇 | 어떻게 | 통과 |
| --- | --- | --- |
| 서명 | `apksigner verify --print-certs <apk>` | `CN=Android Debug`가 **아니다** |
| 키 비커밋 | `git status`, `git ls-files \| grep -i jks` | 아무것도 안 나온다 |
| Metro 없이 도는가 | **Metro를 끄고 USB를 뽑고** 앱을 연다 | `Unable to load script`가 없다 |
| 환경 | 앱 화면 | 「이 빌드는 잘못 만들어졌다」가 **아니다** |

**⚠️ release는 minify·R8·ProGuard가 켜진다.** 동적 `import`(`expoFileSystemPort`·
`onDeviceBackend`·`expoPhotoPort`)와 `llama.rn`의 JNI 심볼이 여기서 깨질 수 있으며,
**debug에서 돌았다는 것은 release에서 돈다는 뜻이 아니다**(원칙 V).

## 테스트

| 명령 | 무엇 | 기기 |
| --- | --- | --- |
| `npm test` | 기기 불필요 갈래 전부 (약 13초) | 필요 없음. **항상 돈다** |
| `npm run test:logic` | 순수 로직만 (**약 7초**) — 개발 중 기본 | 필요 없음 |
| `npm run test:ui` | 화면만 | 필요 없음 |
| `npm run test:device` | 실기기 갈래 (Maestro) | 있으면 돌고 없으면 건너뛴다 |
| `npm run lint` | eslint + tsc + 헌법 검사 + prettier 포맷 검사 | 필요 없음 |

**건너뛴 실기기 테스트는 통과가 아니다.** 기기 없이 전부 초록불이어도 온디바이스는
검증되지 않은 상태다. 기능이 끝났다고 말하려면 최소 한 번은 실기기에서 돌아야 한다
(헌법 원칙 V).

**"최소 한 번"이지 debug와 release를 매번 둘 다가 아니다** (2026-08-22 정리, 012에서
확립). 새 네이티브 모듈이나 빌드 설정(동적 `import`, R8·ProGuard 대상, JNI 심볼 등)을
건드리지 않는 순수 로직·화면 기능이면 **debug 실기기 확인 1회로 충분하다.** release
재확인은 그런 경계를 새로 건드릴 때만 한다 — "debug에서 돌았다는 것이 release에서
돈다는 뜻이 아니다"는 실제로 여러 번 깨졌던 축(005의 `initLlama` JNI, 011의
`initMultimodal` JNI, 006~012의 동적 import)에 적용되는 말이며, UI·로직 변경까지
일반화되는 말이 아니다. 시뮬레이터(Expo Go 등)는 이 프로젝트에서 애초에 옵션이
아니다 — `llama.rn`이 Expo Go에 없고, `local` 환경은 데스크톱 서버 추론이라 dev/prod의
온디바이스 강제 게이트를 애초에 검증하지 못한다.

**⚠️ 새 Maestro 흐름은 `scripts/run-device-tests.mjs`의 `FLOWS`에 등록해야 돈다.**
등록하지 않으면 파일이 있어도 실행기가 돌리지 않고, 그러면 초록불인데 아무것도 검증되지
않은 상태가 된다 — 원칙 V가 막으려는 바로 그 상황이다.

### 테스트가 두 갈래로 나뉜다 — 화면만 RN 런타임을 진다 (2026-08-22 실측)

`jest-expo` 프리셋은 **워커마다 React Native 런타임을 세운다.** 그 비용을 화면을
그리지 않는 스위트 40개가 함께 물고 있었다. `package.json`의 jest 설정을
`projects` 둘로 갈랐다:

| 프로젝트 | 무엇 | 환경 |
| --- | --- | --- |
| `logic` | `__tests__/**/*.test.ts` (40개) | `node` + babel-preset-expo |
| `ui` | `__tests__/**/*.test.tsx` (9개) | `jest-expo` (그대로) |

**실측 (16코어, 웜 캐시):**

| 무엇 | 전 | 후 |
| --- | --- | --- |
| 같은 순수 로직 40개 | 43.8초 | **12.4초** |
| 전체 (`npm test`) | 17~22초 | **12~14초** |
| **`npm run test:logic`** | — | **약 7초** |

**개발 중에는 `npm run test:logic`을 쓴다** — 화면을 안 건드렸으면 787개가 7초에
돈다. 화면을 건드렸으면 `npm run test:ui`, 커밋 전에는 `npm test`다.

- **가르는 기준은 확장자다.** `.tsx`면 화면, `.ts`면 순수 로직이다. 이 저장소에서
  `@testing-library`를 import 하는 `.ts` 스위트는 **0개**이므로 확장자가 곧 경계이며,
  `__tests__/jest-projects.test.ts`가 그것을 못 박는다.
- **새 화면 테스트는 `.tsx`로 만든다.** `.ts`로 만들면 node 환경에 잡혀 `render()`가
  없다고 실패한다 — 오류가 원인을 가리키므로 조용한 실패는 아니다.
- **`transformIgnorePatterns`가 필요하다.** `expo` 패키지가 ESM으로 배포되며, 이것을
  빼면 스위트 4개가 파싱에서 죽는다.
- **`--maxWorkers=50%`는 그대로 옳다.** 75%·100%를 재 보니 **오히려 느렸다**(18초 →
  23.6초 → 27.6초). 워커끼리 CPU를 뺏는다.

#### ⚠️ `testMatch`가 어긋나면 스위트가 조용히 사라진다

**나눈 대가로 새 실패 양상이 생겼다.** 어느 프로젝트에도 안 잡힌 파일을 jest는
**오류 없이 그냥 안 돌린다.** 위반 주입에서 `logic`의 패턴을 좁혀 보니 **33개
스위트·약 700개 테스트가 사라졌는데 「7 passed, 90 tests」로 초록불이었다** —
`FLOWS` 미등록과 같은 종류다.

`__tests__/jest-projects.test.ts`가 파일 수를 직접 세어 막는다.

**⚠️ 그 가드는 일부러 양쪽 프로젝트에 들어 있다.** 처음엔 `logic`에만 두었는데,
패턴을 좁히는 위반을 주입하니 **가드 자신이 함께 사라져 아무 말도 못 했다.**
지키려는 것과 함께 없어지는 것은 가드가 아니다.

### Windows에서 느린 것은 Defender다 (2026-08-18 실측)

**같은 명령이 CI(우분투)에서 6초, Windows에서 11분 39초였다.** 코드도 저장소 설정도
원인이 아니다. `node_modules` 파일을 읽는 데 **처음 35.37ms, 두 번째 0.36ms**가 걸렸다
— **98배 차이**다. Defender 실시간 검사가 파일마다 처음 한 번 가로채고 그 뒤로 캐시한다.
이 저장소의 `node_modules`에는 파일이 44,221개 있다.

- **`eslint .`가 읽는 파일은 75개뿐이다.** `android/`(1,341개)도 `node_modules`도
  lint 대상이 아니다 — **스코프를 좁히는 것은 해결책이 아니다.** 파일 하나만 검사해도
  6.3초가 걸리며, 그것은 eslint-config-expo의 의존성 트리를 여는 비용이다
- 고치려면 `scripts/windows-dev-exclusions.ps1`을 **관리자 권한으로** 돌린다. 저장소가
  아니라 기계의 설정을 바꾸는 것이므로, 돌리지 않아도 CI는 그대로 돈다
- **테스트는 저장소 쪽에서 고쳤다.** `npm test`에 `--maxWorkers=50%`가 붙어 있다.
  16코어에 16워커를 띄우면 워커끼리 CPU를 뺏어 `render()`가 기본 5초 타임아웃을
  넘긴다 — **156초/1개 실패에서 21초/507개 통과로 바뀌었다.** 실패가 매번 다른 파일의
  첫 `render()`에서 났던 것이 그 증거이며, 코드 결함이 아니었다
- **CI에서는 이 값을 쓰지 않는다.** 러너가 2코어라 50%면 워커가 1개가 되어 오히려
  느려진다. `ci.yml`이 `--maxWorkers=2`를 따로 준다

### 실기기 테스트를 돌리기 전에 (실측으로 얻은 것)

세 가지가 갖춰져야 돈다. 하나라도 없으면 **화면에 값이 멀쩡히 있어도 실패한다.**

1. **Metro가 dev 환경으로 떠 있어야 한다** — `EXPO_PUBLIC_APP_ENV=dev npx expo start
   --dev-client`. debug APK에는 JS 번들이 없어 Metro에서 받는다. 없으면 앱이
   `Unable to load script`로 죽고, Maestro는 `launchApp... COMPLETED`를 찍은 뒤 아무것도
   보지 못한다.

   **⚠️ 환경 변수를 앞에 붙이는 것이 핵심이며 `.env.dev` 때문이 아니다**(2026-08-18,
   `node_modules/@expo/env/build/index.js` 직접 확인). Expo는 **`NODE_ENV`로 env 파일을
   고른다** — `.env.${NODE_ENV}` 꼴이다. `EXPO_PUBLIC_APP_ENV`는 파일 *안의 값*이지
   파일을 고르는 열쇠가 아니다. 그래서:

   | `NODE_ENV` | 로드되는 파일 |
   | --- | --- |
   | `production` | `.env.production` ✅ |
   | `development` | `.env.development` ✅ |
   | `dev` | **없음** — 비표준 모드라 경고까지 난다 |

   **`.env.dev`는 어떤 모드에도 매칭되지 않아 자동 로드되지 않는다.** 파일은 있지만
   아무도 읽지 않으며, 위 명령이 도는 것은 변수를 직접 주기 때문이다. 「파일이 있으니
   dev 빌드는 알아서 되겠지」로 읽지 않는다(원칙 V).
2. **기기 잠금이 풀려 있고 화면이 켜져 있어야 한다** — 잠금 화면 위에서는 앱이 뜨지 않는다.
   `adb shell dumpsys trust`의 `deviceLocked=0`으로 확인한다. PIN은 사람이 넣어야 한다.
3. **한글 검증 문구는 `-Dfile.encoding=UTF-8`이 있어야 읽힌다** — Maestro는 JVM이고 흐름
   파일을 플랫폼 기본 문자셋으로 읽는다. 한국어 Windows에서는 CP949라서 `"환경"`이
   `ȯ��`로 뭉개진 채 기기에 전달된다. **실측(2026-08-14)**: maestro 출력 바이트가
   `c8 af b0 e6`이었고 이것은 "환경"의 CP949 인코딩과 정확히 일치했다.
   `run-device-tests.mjs`가 이 옵션을 넣으므로 그 실행기를 거치면 신경 쓸 것이 없다.

## Expo 작업 시

패키지 버전을 추측하지 않는다. `expo install`은 npm이 아니라 Expo API에서 버전을
해석하므로 `npm view`는 틀린 답을 준다. 대상 SDK의 버전별 공식 문서나
context7(`/expo/expo`)로 확인하고 `npx expo install --check`로 검증한다.

## 작업 습관

- 커밋 메시지는 한국어로 쓴다(헌법 「개발 방식」).
- 계약을 먼저 정하고 테스트를 먼저 쓴다(헌법 「개발 방식」).
- **한 축을 깊게 파고들고 싶어지면 그것이 실패 신호다.** 이 프로젝트에서 반복된
  실패는 코딩 에이전트가 여러 축 중 하나를 붙잡고 지나치게 파고든 것이었다.
